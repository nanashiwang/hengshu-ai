# 衡术 Hengshu — 总纲（唯一开发依据）

> 状态：**开发蓝图 · 单一事实来源** · 更新：2026-07-02（五维复盘后重排）
> 本文整合并取代此前 4 份规划稿（产品方向与护城河 / 成品化技术清单 / 生产部署清单 / 联动经济方案），后续开发优化**只依据本文**。
> 历史参考（不在本文范围、保留原文）：`hengshu_prd_v0.2.1.md`(原始 PRD) · `体验手册.md` · `PROGRESS.md`(功能进度基线，已冻结)。
> ⚠️ `hengshu_prd_v0.2.md` 与 `yuanheng_skillhub_product_doc_v0.1.md` 为**定位翻转前**旧稿（自部署时代），**勿作依据**，仅存档。

---

## 1. 产品定位

**衡术是一个中心化运营的 AI Skill 平台，"决策层 + 履约层"一体机**：
- **决策层**：中立评测 + 省钱路由——用真实运行数据告诉用户"这个 Skill 在哪个模型上真省真稳"，并自动帮你选当前实测最省够用的模型。
- **履约层**：作者自有的 **New API 中转**——模型调用真正发生的地方，平台从 margin 赚钱。
- **算力 = 用户自带 Key（BYOK），默认走自己中转**；平台不承担现金算力成本，从中转 margin 变现。
- **公网多用户中心化**（非自部署）；单一中心 → 兼容报告/术值榜/求术全站聚合，PT 网络效应成立，信任模型为中央签名。

---

## 2. 护城河与竞争力（战略核心）

**诚实前提**：单纯"攒兼容数据"不是护城河——结论低密度可爬、评分不奖励样本量、网关在数据上游、数据会腐坏、看一眼就走。**护城河必须挪到"长在真实 BYOK 流量上、且创造切换成本"的地方。**

**加固后的 5 层护城河**（均以已有代码为基座；标注"目标态"的部分尚未实现，勿当现状引用）：

| 层 | 是什么 | 为何抄不走 |
|---|---|---|
| **0 活体数据** | `compat.ts` 聚合层：现状=modelName 单级分组+时间衰减(halfLife=30天)+置信度折减；**目标态(阶段4)**=(modelName,modelVersion) 二级分组+漂移曲线 | 竞品抓走"今天"，抓不走连续过去与跨版本漂移曲线；腐坏反成壁垒 |
| **1 省钱路由** ⭐ | **目标态(#15)**：`recomputeLocalScore` 回写 `routePolicy`，绑 BYOK 后自动选实测最省够用模型、漂移自动换 + console"省钱回执"。**现状：routePolicy 为作者手填静态 JSON，回写未实现** | 抄"看表"易，抄"真实回流驱动的自动降本路由"须先有等量真实运行 |
| **2 私人台账** ⭐ | 激活 `SkillRuns`（已存 input/output）做"我的运行台账 + 换模型一键重跑" | 用户**自己的**数据，非公共平均；迁移成本随使用线性上升；堵死 BYOK"查完去网关直连"漏点 |
| **3 任务可靠性** | 聚合键从 modelName 下移到 profile(skill×输入档×errorType)；代际回归告警 | 模型趋同后不消失反更值钱（prompt 工程质量差异不趋同） |
| **4 可验签基准** | verified + ed25519 升级为第三方达标证书；黄金样例逐条打分 | 网关自评自证有利益冲突，中立性结构上是你的 |

**⭐ 最锋利竞争力（复购与切换成本唯一来源）= 第1层×第2层**：
> **"绑你的 Key 后，用你自己历史 prompt 的真实回流，持续替你选最省的模型跑、漂移自动换路由、并累计告诉你这月省了多少钱。"**
> 个人化 + 每周涨可推送 + 离开就丢台账——省的钱沉淀在平台账本而非用户脑子里。

### 2.1 六维红队复核定论（2026-07-02：6 类候选 × 竞品红队，全部按最狠打法攻击）
**残酷事实：没有任何一层"今天"就成立**（评级：切换成本=C，数据/网络效应/信任/成本结构/标准=D）。但每层的成立开关都已定位，多为天级工作量。核心定论：

1. **数据不是独立护城河，是第1×2层的派生物**。壁垒深度公式 = **独占流入速率 × 半衰期**，不是累计条数。规模阈值有算术：全站需 ~77 次真实运行/天才能让 25 Skill×4 模型的兼容矩阵整体"满置信活着"——低于此值护城河深度记零，不对外宣传。"网关没有 Skill 语义"防线偏弱（response_format/json_schema 可让网关推出等价 formatValid），真独占的只有：**私人台账（ownerOrAdmin 天然不可爬）+ 真实用户自然流量分布 + Skill 语义连接键**。
2. **两面承重墙（红队打不穿的仅有位置）**：① **个体切换成本**——SkillRuns 私人台账 × 省钱回执（累计省钱额是唯一**不随 30 天半衰期归零**的单调增长资产，留存钩子全押它）；不需要网络密度，solo+¥500/月唯一养得起的形态。② **跨中转中立元台账**——国内 New API 中转极度碎片化，没有任何单一网关看得到用户跨中转的全量调用；衡术定位为"跨中转的中立元台账/元路由"是**唯一对"网关上游超集"攻击免疫**的结构位置。
3. **counter-positioning 只对"自有模型利益"平台成立**（Coze/豆包、百度系抄真诚中立=自噬），**对中立聚合网关不成立**（OpenRouter 本就中立+有 :floor 价格路由，AWS Cost Explorer 证明"教客户省钱换留存"不是禁区）。低固定成本=氧气不是墙：它买的是熬到第1×2层长成的 12-24 个月时间。
4. **信任层单独不成立，只能当第0-2层的"信用放大器"**，且必须机器强制而非口号：四面墙目前 0/4 落地——"历史长度"因子从落地日才开始计时，**每晚一天轨迹短一天**。先自曝 margin 政策（披露在先=透明，被揭在后=丑闻）；SkillRuns 明文 input/output 是埋在第2层的**信任毁灭开关**（一次泄漏全层陪葬）。
5. **冻结两个叙事**：❄️"推 Skill Spec 事实标准"（solo 无分发杠杆+大厂格式收敛窗口期，最差时点；改做 import 适配器搭 Claude Skills/GPTs 便车，fork 谱系按"变异-表现差分数据采集器"立项）；❄️"网络效应"（唯一半通电的数据回路电流≈运营者本人；冷启动期不当卖点）。PT 邀请制**分层收缩**：只锁"挣术值/verified 计分/兑换"经济写路径，读+限额试跑放开流量（online 权重 0.3+封顶 3 条+6a 扣费已兜底）。

---

## 3. 经济模型（变现，且反哺护城河）

### 3.1 三币单向漏斗
```
人民币 ──充值──▶ credit(算力燃料) ──跑模型──▶ 蒸发
贡献行为 ──挣──▶ 术值 ──受控兑换──▶ credit
                     术值/credit 永不反向变现金 ❌（保命红线：防资金盘/赌博）
```

| 币 | 角色 | 护栏 |
|---|---|---|
| CNY | 唯一入金口（New API 兑换码充值） | 衡术不碰现金；无任何→CNY 出口，代码硬禁止 |
| credit | 唯一花算力的币(1credit=¥0.01零售) | 物理映射为受限子令牌（低价分组+配额熔断+TTL）；free 档不产术值/不可兑换转让→白嫖上限=进货成本，套现≈0 |
| 术值 | 贡献信誉分（不可买只能挣） | 高价值行为**被采纳才发分**；不可转赠；永不进随机池 |

**核心不变量（结构性防亏）**：术值兑换池 = **α × 当月已实现毛利**(α=0.30) → 先赚到才有得兑，**永不亏 margin**，最坏少赚 30%。

### 3.2 抽奖的最终结论：**不做纯抽奖，做"满额返利宝箱"**
纯抽奖/开箱踩两条红线（反不正当竞争法有奖销售 + 变相赌博）。改：**充值满额 100% 必得**确定返利(6% 公示)，**随机只决定返利形态**（都是额度券，用户永不空手）——类"第二杯半价+随机送小料"。术值绝不进随机池。⚠️ 上线前走一次律师。

### 3.3 中立与变现共存：四面墙（护城河不被变现稀释）
1. **数学隔离**：`modelrank.ts` 排名函数入参**无 margin/isOurs 字段** + CI `grep` 断言（人事后也改不动）。
2. **记账隔离**：benchmark 按成本价记账(margin=0)；履约 SkillRuns 不进榜分。
3. **成本真值隔离**：用公开官方报价快照(`ModelPriceSnapshot`)非平台可编辑价；双列"官方价 vs 我到手价"，更贵必红标"BYOK更省"。
4. **履约隔离**：run 响应强制回传 `cheaperViaByok`，更省走 BYOK 不得隐藏。

### 3.4 New API 对接
1 个 New API 账号 = 全体用户，靠**子令牌**隔离（`token_name="hs_<userId>"`）。**已定架构（优于原稿）**：权威 credit 账本在本平台 `credit-logs`（原子事务+幂等），网关配额同步是提交后的**副作用**（失败不影响账本，靠对账 worker 补偿）——而非原稿"先扣术值→PUT /api/token→失败回滚"的跨系统事务。注册发受限子Key(60秒真跑)；cron 拉 `/api/log` 用量真值回流喂护城河(剥离明文)并回填当月毛利。

---

## 4. 现状与差距

### ✅ 已建成（S1-S7 + 信任模型，代码核实真跑通）
下载→安装→离线运行(Runner CLI)→兼容回流+LocalScore→术值/贡献2.0→求术悬赏→manifest ed25519 签名；前台首页/市场/详情/排行/悬赏/控制台；评论收藏闭环。
**本会话已修**：求术钱路原子化+幂等 · Media 本地卷 · DB idle 超时。

### ❌ 关键差距（2026-07-02 五维复盘后的真实现状；历史红项已修见 §5 各✅）
> 已修不再列：创作者供给断路✅(#7) · register 印术值✅(#1) · 数据一致性批次A+对账✅(#5) · 测试CI基线✅(#20) · 前端基建✅(#19)。

| 缺口 | 严重度 |
|---|---|
| **🚨 credit 漏斗断裂**：无充值入口(recharge 零实现)、无消费出口(runSkill 只记 chargedAmount 不扣钱)——兑换(#13)只建了中段，credit 是发得出花不掉的死币；且兑换池毛利为手填占位 | 🔴🔴 资金红线（exchangeEnabled 未接真值前**禁止开启**） |
| **🚨 烧钱端点无护栏**：run/compare 登录即用、无限流无配额，用户无 BYOK Key 时静默回退**平台全局 Key**→ 生产配上真 Key 后任何注册用户可无限烧平台现金（compare 一次并行 4 模型） | 🔴🔴 配真 Key 前必修 |
| **SkillVersions.update 越权**：creator 角色可 PATCH 他人 Skill 版本的 prompt（劫持在线运行；已下载签名制品不受影响） | 🔴 |
| 删用户不处理 credit 台账（余额蒸发、流水悬空 user=NULL）；对账 worker 未覆盖 creditBalance/favoriteCount/avgRating | 🔴 |
| 收藏↔取消循环刷 skill_favorited 术值（发放无幂等、删除不回收，50/天/号） | 🔴 |
| **封禁机制全缺**：Users 无 status/banned，管理员对恶意用户唯一手段是删号，而删除保护恰让恶意作者最难删 | 🔴 |
| Runner token 明文存储；设备码可钓鱼绑定(authorize 无来源确认+token 无鉴权轮询)；register 原样回显内部错误(可枚举账号) | 🟠 |
| 通知系统 / 用户设置(自助改Key) / 举报处置端 缺失 | 🟠 |
| 高频查询列全无索引(skills.status/skillRank/runCount、users.contributionScore)——现在加是一行配置，之后要走生产迁移 | 🟡 |

---

## 5. 统一落地路线（唯一开发顺序）

> 合并了原部署/成品/护城河/经济四份的所有优先级。按阶段推进，每阶段独立冒烟。

### 🚨 阶段 0 · 立即止血 + 护城河地基（本周，多为 0.5-2 天/项，不依赖外部决策）
1. **修 register 术值漏洞** ✅限速已做(2026-07-02，对抗审查确认正确无回归)：register 改为依赖规则表(不传 points)；invite/skill_version_update 加 `dailyLimit=3`。⚠️ **残余(未根除)**：① 自邀刷分——invitee≠inviter 使 `selfActionExcluded` 失效，需 #6 设备/IP 去重才能堵死。
1b. **修 skill_published(+50) 反复发布刷分** ✅已修(2026-07-02，对抗审查通过)：afterChange 按 Skill 幂等(查 contribution-logs 已有 skill_published 则不再发)，反复 published↔非published 不再重复奖励。残余[低]：并发双发竞态(check-then-act 非原子)。注：全局 `(relatedSkill,actionType)` 唯一约束会误伤 skill_run/favorited 等每 Skill 多条日志的行为，**不可行**；正解是 partial unique(仅 WHERE actionType 为一次性行为)或保持现有应用层幂等(已足够，竞态极低)。
2. **`compat.ts` 聚合层重写** ✅已做(2026-07-02，对抗审查通过)：时间指数衰减加权(halfLife=30天，近期主导抗腐坏) + LocalScore 置信度衰减(有效样本<10 则按比例压分，杜绝 N=1 的 100% 噪声霸榜) + decayWeight 缺时间戳按 0 权重(防"无时间=最新"刷分)。**残余**：① confidence 对长期无回访的旧优质 Skill 会指数压到 0(与"无数据"不可区分)，考虑加地板或区分展示 ② limit:5000 全量+同步重算是性能定时炸弹，改时间窗(近180天)拉取+异步重算可一并治旧数据腐坏 ③ (modelName,modelVersion) 二级分组/漂移曲线留阶段4。
3. **水龙头·在线评测回流** ✅已做(2026-07-02，对抗审查通过)：在 `runSkill` 里为真实非 mock 调用回流 `source='online'` 兼容报告（不含原文，仅分档/成败/耗时），在线试用与多模型对比都喂护城河。**抗刷三重**：排除作者自跑 + 单用户对同一(skill,model)封顶3条 + 来源分级权重(verified=1/community=0.5/**online=0.3**)削弱刷分投毒杠杆。**残余**：作者用小号仍可绕自跑排除(需 #6 设备/IP 去重根治)；localScore 缓存不在热路径刷新(靠 report/worker 收敛，反而更安全)。
4. **最小样本门槛** ✅已做(2026-07-02)：单模型报告<5 则前端显示"战绩积累中·已N次"(colSpan 跨三列)而非误导性百分比(`aggregateByModel` 返回 lowSample，详情页据此渲染)。
5. **数据一致性地基**：✅**批次A已做**(2026-07-02，对抗审查通过)——`Favorites(user,skill)`/`Reviews(user,skill,type)`/`SkillInstalls(user,skill,runner)` 复合唯一约束 + favorite/install 竞态兜底(回查确认再兜底) + `Categories`/`RunnerClients` beforeDelete 清孤儿(含 device-codes) + ReviewForm 唯一冲突友好提示。⚠️ **上线前必须先去重**：favorites/reviews/skill-installs 若有历史重复行，dev push 建唯一索引会失败（无 migrations），先跑去重 SQL。**残余**：① ✅ **Users 删除级联已做**(2026-07-02，对抗审查通过：阻断有作品/进行中悬赏者 + 删私有从属触发计数修正 + 解引用保留历史；补全 reports/skill-versions 遗漏外键；原子回滚。Payload postgres `_rels` cascade 特性已确认——遗漏字段是静默丢引用而非删不掉) ② `SkillInstalls.runner` 可空致 NULL 组唯一失效（当前调用链恒非空，潜在）③ ✅ **指标可从 SkillRuns 重算已做**(2026-07-02，对抗审查通过)：`worker:reconcile` 台账对账 worker——术值重算为 SUM(logs.points)、Skill 指标由 skill-runs 唯一重算，默认 dry-run/`--apply` 写回；配套 SkillRuns 加 `countedInMetrics` 持久化"是否计入指标"意图(compare 探测=false)防错计；剥离 rank(依赖 wall-clock 非可重算) ④ ✅ **余额并发已大幅缓解**(2026-07-02)：awardContribution 结算金额守卫(bounty/consume/other 绕过规则表防误伤) + update+create 原子化(自开事务保 `contributionScore==SUM(logs)` 不变量)；真正 SELECT FOR UPDATE 行锁仍未做，但"原子写入 + 对账 worker 周期性纠偏"已覆盖 read-modify-write 丢更新的实际风险。
6. **反女巫地基** ✅ipHash 已做(2026-07-02，对抗审查通过)：Users 加 ipHash(HMAC不可逆/admin-only/hidden)；register 采集 IP(**从右往左取 XFF 防伪造**+`TRUSTED_PROXY_COUNT`) → 同 IP 24h 注册宽松频控(20，避 CGNAT 误伤，含容错) + 同 IP 自邀不发术值(根治小号刷邀请)。**残余**：① XFF 需生产反代 append 真实对端才可信(部署时配) ② deviceHash/前端指纹未做(能防同 IP 之外的多开) ③ ipHash 仅注册快照、跨网可绕 ④ PAYLOAD_SECRET 缺失回退固定盐(并入 env 校验项) ⑤ 同网真人互邀不计术值(需 FAQ 明示)。

### 🚨 阶段 0.5 · 复盘补洞（2026-07-02 五维审查+对抗核验产出；先于阶段1 剩余项）
6a. **credit 消费出口 + 烧钱端点护栏**（两洞同根，一并修）：runSkill 无 userApiKey（走平台 Key）时要求 `creditBalance ≥ 预估成本`，成功后 `applyCredit(type:'consume', -cost, idempotencyKey:runId)` 原子扣减，余额不足 402；BYOK 自付不扣；mock 不扣。附带每用户频控。→ 同时落地"三币漏斗出口"与"平台不烧钱"红线。
6b. **SkillVersions 越权修复**：update/create 按所属 Skill 的 author 归属过滤（对齐 Skills 的 Where 模式），杜绝 creator 改他人 prompt。
6c. **Users 删除级联补 credit**：creditBalance≠0 或有流水者阻断删除（同"有作品阻断"模式）；对账 worker 扩展 ③creditBalance==SUM(credit-logs) ④favoriteCount/reviewCount/avgRating 重算。
6d. **skill_favorited 幂等**：ContributionLogs 加 actor 字段；同 (actor,skill,actionType) 一次性发放、取消不回收（对齐 skill_published 模式）。
6e. **封禁机制**：Users 加 status(active/banned) + 登录/auth 拦截 + 封禁时其 compat-reports 降权、冻结兑换；Reports 加 admin 处置动作。
6f. **安全杂项**：register 错误改通用文案(防枚举)；runner token 改哈希存储+TTL；device authorize 加来源确认展示+token 轮询频控；run/compare 不透传上游错误原文；bounty submit 校验交付 Skill 归属。
6g. **热路径索引**：Skills(status,visibility) 复合 + skillRank/runCount/successRate + Users.contributionScore `index:true`（趁 dev push 免迁移窗口）。
6h. **迁移基线提前**：阶段1 收尾时 `payload migrate:create` 生成基线快照，此后 schema 变更走 migration；去重 SQL 写成首个数据迁移。
6i. **封 CompatReports 时序外泄**（红队新发现，0.5天）：`read: ()=>true` 使原始报告行含 createdAt 可被 REST 全量导出——"时序抓不走"当前为假。收敛为 admin-only（详情页走服务端 `aggregateByModel` 不受影响），对外只暴露当前窗口聚合结论。
6j. **四面墙机器强制起步**（小时级+1-2天，"历史长度从落地日计时"）：① CI 加 grep 断言（skillrank/modelrank 命中 margin/isOurs 即红）② localScore 回写时落 ed25519 签名的 append-only 快照集合（create-only），改历史分数必留痕；快照哈希外锚（公开 git 仓库）。
6k. **关 Runner 未签名静默放行洞**（0.5天）：`hengshu.mjs` 对无 checksum/未签名 manifest 从"静默放行"改为拒装（`--allow-unsigned` 显式豁免），防克隆站镜像换签重分发。

### 阶段 1 · 供给命脉 + 护城河数据源（成败关键）
> **复盘定论（依赖倒置纠偏）**：#13 兑换已建成但其价值依赖"术值有意义"→依赖"有作者产出+真实运行"。剩余项按北极星杠杆重排：**#8 发布即评测 → #9/#10 冷启动 → #15(前半:routePolicy 数据回写) → #16 台账+#17 通知(合并一迭代,共享控制台读视图) → #12 四面墙 → #14/#18 推迟**。
7. **前台"发布 Skill"表单** ✅已做(2026-07-02，对抗审查通过：安全/越权/原子/回归全过)：`POST /v1/skills`(登录即可，status 硬编码 pending、author 硬编码本人、白名单字段防注入)事务内建 Skill+首版本、版本 afterChange 自动设 currentVersion；前台 `SkillForm`+`/console/skills/new`+侧栏入口；单作者 pending 存量上限 20 防刷。**残余**：① ✅ "我的作品"页(A4，`/console/skills`)+ 草稿预览(A5，作者/审核员可在详情页预览自己未发布 Skill) 已做(2026-07-02，安全审查确认不泄露) ② 无幂等键，超时重试会生成不同 slug 的重复 Skill(低危，审核期人工去重)。
8. **发布即评测**：发布/fork 后自动跑一轮 compare 生成初始 LocalScore。
9. **5 种子标"可 fork 模板"** + 首页"从改一个现成的开始"。
10. **作者亲手灌 20-30 个满数据 Skill**（几十块 API 费，跨过飞轮静摩擦）。
11. **New API 子令牌联动**：🟡 **管理接口抽象已做**(2026-07-02)：`src/lib/newapiAdmin.ts` 抽象 provisionSubToken/adjustQuota/fetchUsage，stub 模式(默认)模拟成功使变现闭环本地可跑通，real 模式在作者提供真实 curl 前抛 NotImplemented。**待作者**：贴一次后台建令牌/看日志的 Network 请求 → 填充 RealAdmin；cron 拉 `/api/log` 落评测真值表；网关配额对账 worker。
12. **中立四面墙**：`modelrank.ts`+CI断言 · `ModelPriceSnapshot` · benchmark margin=0 · run 加 `cheaperViaByok` · 双列价 · 中立看板。

### 阶段 2 · 变现闭环 + 最锋利竞争力
13. **术值换 credit 兑换池**：✅ **引擎已做**(2026-07-02，金融级对抗审查通过)｜⚠️ **但闭环未成**：credit 入口(充值)与出口(消费,见 6a)缺失前，兑换=发不可花的 IOU；且池毛利为手填占位——**exchangeEnabled 在 6a 落地+毛利接真值前禁止开启**。
    - 三币地基：Users.creditBalance + `credit-logs` 台账(不变量 creditBalance==SUM(logs)) + `src/lib/credit.ts` applyCredit(原子/幂等/防透支)
    - `economy-settings` 全局(enabled/α/当月毛利占位/兑换率/三层上限) + `src/lib/economy.ts`(池核算)
    - GET/POST `/v1/economy/exchange`(状态+兑换) + 控制台兑换 UI
    - 保命红线落地：兑换池=floor(α×当月已实现毛利分)，credit与分1:1，先赚到才有得兑
    - **对抗审查 P0 修复(真机验证)**：pg_advisory_xact_lock 全局串行化兑换关键区→事务内复核池/术值余额，杜绝并发池超支(亏margin)与术值透支(凭空造credit)；awardContribution 加负余额保护；强制 idempotencyKey；clamp α∈[0,1]
    - **残余**：① 网关配额对账 worker(切 RealAdmin 前补，防"credit已发网关无配额") ② 术值/credit 账本并发交错的集成测试 ③ `monthlyRealizedMarginCents` 现为手填占位，接 New API `/api/log` 前务必核对单位(分)勿填成元(放大100倍) ④ 术值行锁靠全局咨询锁串行(兑换低频可接受)
14. **术值明码标价商城**：⏸ **推迟**（复盘定论：北极星①无基线时曝光是零价值商品；且 verified 插队与护城河第4层信任标记冲突——只卖曝光类不卖信任类）。
15. **省钱路由落地**（护城河第1层⭐，**当前数据驱动为零**——selectModel 读的是作者手填静态 JSON，recomputeLocalScore 从不回写 routePolicy）：**拆两步提前**——①先在 recomputeLocalScore 按模型聚合成败/成本回写 `version.routePolicy.strategies.cheap`（复用已有聚合，1-2天）②"省钱回执"UI 后置。数据前置依赖 #8/#10。
16. **私人运行台账**（护城河第2层⭐）：SkillRuns 台账 + 换模型一键重跑；Favorites/Installs 升级为订阅告警。**与 #17 通知合并为同一迭代**（共享控制台读视图；通知事件源 ContributionLogs/SkillRuns/Favorites/Bounties 全部已在写，只缺 notifications 集合+小铃铛，是"周活作者"最强回访钩子）。
17. **通知系统 + 用户设置(自助改模型Key) + 举报处置端**（并入 #16 迭代）。
18. **返利宝箱**：⏸ **移出主线**——前提(充值 recharge)零实现；改挂"recharge 跑通"之后作条件项，**需法务 review**。

### 阶段 3 · 成品化（UX + 质量 + 上线）
19. **前端基建**：✅ 移动导航(汉堡菜单 `MobileNav`) · ✅ error/loading/not-found 边界(全站前台) · ✅ SEO(sitemap/robots + 详情页 generateMetadata，2026-07-02；⚠️用 NEXT_PUBLIC_SERVER_URL 受 build 期内联影响，待 SERVER_URL 去内联) · ✅ **列表分页**(2026-07-02，对抗审查通过：共享纯服务端 `Pagination` 组件[首尾±1 窗口/自定义 pageKey 支持一页多榜/越界 page 内部 clamp 防坏链接]；覆盖 Skill市场·悬赏·排行双榜·控制台6tab·我的作品；筛选/排序/搜索切换重置页码) · ✅ **一键复制**(2026-07-02，`CopyButton` 内联"✓已复制"反馈+非HTTPS execCommand回退；接入邀请码分享/校验和核验两处) · ✅ 移除登录页种子凭据(admin12345/WELCOME1，2026-07-02)。**残余[P2]**：详情页评论(limit20)/版本(limit10)未分页，热门 Skill 看不全历史——非命脉，后续做"加载更多"；全局 Toast Provider 未做（当前内联反馈已够，按需再上）。
20. **测试 + CI**：✅**基线已做**(2026-07-02)——Vitest 地基(vitest.config/@别名/node环境) + 6 文件 37 例覆盖对抗审查修过的安全/正确性纯函数(XFF右取防伪造·canonical确定性·ed25519签名验签往返·bucketSize/anonHash·selectModel·分页窗口) + GitHub Actions CI(node22·npm ci·typecheck·test 确定性绿)。**残余**：① 术值账本集成测试(需测试库/Payload Local API，未做) ② CI 的 lint+build job(依赖 DATABASE_URL/PAYLOAD_SECRET 等 env，待配 secrets) ③ 分页纯逻辑已抽到 `src/lib/pagination.ts`(SRP)。
21. **部署上线**：schema 迁移体系 · 密钥强随机 · HTTPS/域名 · cookie Secure · 首管强口令 · 备份 · 健康检查（详见 §6）。
22. ✅ **README 重写**(2026-07-02：更正 v0.1→当前能力/18集合/首用户引导/指向总纲) + ⏳ CHANGELOG/版本纪律。

### 阶段 4 · 抗趋同 + 打磨
23. 任务可靠性层 `aggregateByProfile` + 代际回归告警（护城河第3层）。
24. 可验签中立基准证书 + 黄金样例逐条打分（护城河第4层）。
25. 白标/i18n/无障碍/评论管理/分类页 等增强。

---

## 6. 部署上线要点（平台相关，阶段3 内落地）
仍必须：**schema 迁移体系**（生产关 push、启动前 migrate）· **密钥强随机**（你自己配一次）· **HTTPS+域名** · **cookie `Secure`**（`Users.auth.cookies`）· **首管强口令**（走 Payload 首用户引导，删文档明文弱口令）· **备份**（pgdata+media 一致性）· **健康检查**（`/health`+compose）· **限流/CSRF**（公网多用户升为必做）。
> 定位翻转后已**移除**：LICENSE 开源协议、密钥首启自举、多架构镜像、换域名不重建、面向陌生部署者文档。
> ✅ 已修：Media 本地卷 · DB idle 超时。

---

## 7. ⚠️ 待作者拍板（否则无法动阶段2 的"钱"）

**A. 真实数值（决定全盘，上线前用你实例小额真兑校准）**：New API 各模型**真实 margin 率** + **quota 刻度**（1 美元=多少 quota）；α(0.30)/β(0.15)/免费额度 F(30-50credit)/术值兑换率。
**B. New API 能力（贴一次真实 curl）**：是否 QuantumNous/new-api；建 token 能否设 group/remain_quota/expired_time(TTL)/日调用帽/model_limits；`/api/log` 能否读单用户消费明细+进货成本。
**C. 合规法务（3 件，必做）**：满额返利+虚拟奖是否规避有奖销售/赌博红线；credit/术值不可提现倒卖的协议条款；额度充值是否算单用途预付卡需备案。
**D. 中立性取舍（书面确认）**：接受榜单如实标"我更贵、BYOK更省"？加速/曝光绝不进随机池仅明码标价？是否愿花钱代跑竞品模型 benchmark？
**E. 反女巫**：deviceHash/前端指纹是否补（ipHash 已落地✅）？verified Runner 开通方式（人工/保证金/实名）？

---

## 8. 北极星指标 + 残余风险
**北极星**（这两个不起来一切空转）：① 每周有产出的作者数 ② 单位 Skill 累计真实样本数 × 当前窗口新鲜度。（非 Skill 总数/PV 虚荣指标）

**残余风险**（外生、产品无法对冲）：分发即正义（网关顺带看板截流）· solo 在真实用户量的资本消耗战处下风 · 隐私承诺(只存 sizeBucket)封顶护城河深度且与私人台账黏性冲突 · verified 手动置位是规模瓶颈 · 合规最终定性需属地律师 · 冷启动烧固定预算(P_boot+Free_boot≈¥500/月)。

---

## 9. 关键代码锚点（build 时直接定位）
| 事项 | 文件 |
|---|---|
| 聚合层重写(护城河第0层) | `src/lib/compat.ts`（aggregateByModel/recomputeLocalScore） |
| 水龙头/线上评测回流 | **`src/lib/skillRunner.ts`（6b 块，source:'online' 回流+抗刷）**；`compare/route.ts` 仅调用方 |
| register 术值漏洞 | `src/app/v1/auth/register/route.ts` + `src/lib/contribution.ts` + `src/collections/ContributionRules.ts` |
| 省钱路由 | `src/lib/route.ts`（selectModel）+ `routePolicy` + `MODEL_PRICES` |
| 私人台账 | `src/collections/SkillRuns.ts`（inputJson/outputText/model/estimatedCost） |
| New API 对接 | `src/lib/newapiAdmin.ts`（管理 API 抽象，stub/real 双模式✅）· `src/lib/newapi.ts`（chatCompletion） |
| 中立评测榜(新建) | `src/lib/modelrank.ts` + `ModelPriceSnapshot`(新集合) |
| 签名/验签 | `src/lib/signing.ts` · `canonical.ts` · `runner/hengshu.mjs` |
| 术值/悬赏事务范式 | `src/lib/contribution.ts` · `src/collections/Bounties.ts`(frozenPoints/throwOnError/idempotencyKey) |
| 创作者发布 ✅已建成 | `src/components/SkillForm.tsx` · `src/app/(frontend)/console/skills/new/` · `POST src/app/v1/skills/route.ts` |
| credit 台账/兑换 ✅ | `src/lib/credit.ts` · `src/lib/economy.ts` · `src/app/v1/economy/exchange/route.ts` · `src/collections/CreditLogs.ts` |
| 台账对账 worker ✅ | `src/worker/reconcile-ledger.ts`（dry-run/--apply） |

---

## 10. 一句话收尾
**别人复制得了我的静态结论，复制不了我"持续为具体用户的具体 prompt 生成、越用越贵的活体运行资产 + 中立可验签基准 + 自有中转的省钱路由变现闭环"。眼下最优先的两件事：① 阶段0.5-6a——补上 credit 消费出口与烧钱端点护栏（配真网关 Key 前的资金红线）；② 阶段1-#8 发布即评测——让每个新 Skill 自动获得初始数据，供给飞轮才能转起来。**
