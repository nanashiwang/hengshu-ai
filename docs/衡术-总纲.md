# 衡术 Hengshu — 总纲 v2

> 状态：开发蓝图 · 单一事实来源
> 更新时间：2026-07-08
> 定位：AI Skill 的可信与兼容控制平面
> 本文取代旧版“省钱路由 + 履约一体机 + 术值经济”主叙事。后续产品、代码、文档和运营动作均以本文为准。

---

## 0. 一句话

**衡术 Hengshu 让 AI Skill 像软件包一样拥有身份、版本、签名、兼容证据、失败记录和企业治理能力。**

用户不是为了一个 Skill 去换模型。

衡术真正要解决的是：

> **让 Skill 适配用户已经选择的模型、网关、本地 Runner 和企业环境。**

---

## 1. 衡术是什么，不是什么

### 1.1 衡术是什么

衡术是 **AI Skill 的可信与兼容控制平面**。

它服务四类对象：

| 对象        | 核心价值                         |
| --------- | ---------------------------- |
| 普通用户      | 找到可信 Skill，并在自己已有模型上稳定运行     |
| 开发者 / 创作者 | 发布、测试、签名、适配、维护 Skill         |
| 企业        | 建立内部 AI Skill 注册表、审批、审计和治理体系 |
| 平台生态      | 积累跨模型兼容证据、失败知识库和适配补丁库        |

### 1.2 衡术不是什么

衡术不是：

* 单纯的 Skill 下载站；
* Prompt 市场；
* 通用模型网关；
* 靠 New API margin 打价格战的平台；
* 靠排行榜和下载量制造虚假繁荣的社区；
* 以“用户为了 Skill 换模型”为前提的模型路由产品。

### 1.3 产品主叙事

旧叙事：

> 这个 Skill 在哪个模型上真省真稳，平台帮你选最省够用的模型。

新叙事：

> 你已经有模型了，衡术让 Skill 适配你的模型，并持续证明它是否可信、是否稳定、是否可治理。

### 1.4 对客户的宣传口径

对外不要把衡术说成“Prompt 市场”“模型网关”或“省钱路由站”。

当前应宣传为：

> **一个让 AI Skill 可验证、可试跑、可换模型、可进企业治理的可信兼容平台。**

客户能直接获得的帮助：

| 客户类型 | 可宣传能力 | 直接价值 |
| --- | --- | --- |
| 普通用户 | 必备 Skill、Passport、兼容矩阵、在线试跑、私人运行台账、换模型重跑 | 快速尝到效果，知道哪个 Skill 在自己的模型上能稳定用 |
| 创作者 | Skill 发布、Contract、Passport、证书、失败反馈、Adapter 草稿 | 把一次性 Prompt 变成可维护、可证明、可被企业采用的 Skill |
| 企业 | Enterprise Registry、审批、模型白名单、审计、企业失败库、企业内 Passport | 管住哪些 Skill 能用、谁批准、在哪些模型上验证过、出错怎么追踪 |
| 平台生态 | 兼容报告、ModelProfile、FailureCase、AdapterProfile、EvidenceSnapshot | 把真实运行回流沉淀成长期可信与适配资产 |

---

## 2. 用户路径

### 2.1 普通用户：在线使用 Skill

```text
用户打开 Skill 页面
→ 查看 Skill Passport
→ 查看当前模型兼容性
→ 输入任务内容
→ 平台在线运行
→ 返回结果
→ 记录运行是否成功、格式是否有效、成本与延迟
→ 更新兼容证据
```

在线运行支持两种模式：

| 模式   | 说明                                |
| ---- | --------------------------------- |
| BYOK | 用户提供自己的模型 API Key，平台只负责调度与记录      |
| 平台代付 | 走平台配置的模型网关 / New API 子令牌，用于试用和商业化 |

平台代付是履约能力，不是核心护城河。

---

### 2.2 开发者 / 高隐私用户：本地 Runner 使用

```text
用户安装 Hengshu Runner
→ 在平台选择 Skill
→ 下载签名 manifest
→ Runner 校验 checksum 和签名
→ 绑定本地模型或 OpenAI 兼容 endpoint
→ 本地运行
→ 可选择回传兼容报告
```

本地 Runner 的价值：

* 用户数据不必离开本地；
* 企业可在内网使用；
* 用户可使用自己的模型网关；
* 平台获得脱敏兼容数据；
* Skill 可以跨模型环境复用。

---

### 2.3 创作者：上传与维护 Skill

```text
创作者上传 Skill 包 / Prompt Skill
→ 平台规则扫描
→ AI 审核
→ 生成人类可读说明
→ 生成或更新 Skill Contract
→ 生成 Skill Passport 初稿
→ 进入 pending / published / rejected
→ 后续根据运行数据更新兼容性和失败记录
```

创作者获得的不是“上传一个 Prompt”，而是：

* 可验证的 Skill 身份；
* 版本化维护能力；
* 跨模型适配能力；
* 失败反馈；
* 可信分发；
* 企业采用机会。

---

### 2.4 企业：AI Skill Registry

```text
企业导入 Skill
→ 查看 Passport、权限、风险、兼容模型
→ 管理员审批
→ 进入企业私有注册表
→ 配置模型白名单与使用范围
→ 员工使用
→ 运行记录进入审计
→ 版本可回滚、可禁用、可替换模型
```

企业版核心不是“更多 Skill”，而是：

* 哪些 Skill 可以用；
* 谁批准的；
* 在哪些模型上验证过；
* 出错如何追踪；
* 是否能换模型后继续跑；
* 是否有审计、权限和版本治理。

---

## 3. 核心对象定义

### 3.1 Skill

Skill 是一个可复用 AI 能力单元。

它可以是：

* Prompt Skill；
* 结构化输出 Skill；
* 工作流 Skill；
* 工具型 Skill；
* 企业私有 Skill。

但 v2 阶段优先支持：

> **低风险 Prompt / 结构化 Skill。**

高风险 Skill，包括网络、文件读写、Shell、脚本、外部服务调用，必须进入人工审核或企业私有环境。

---

### 3.2 SkillVersion / Skill Contract

`SkillVersion` 是 Skill 的具体版本。

它至少应描述：

* system prompt；
* user prompt template；
* input schema；
* output schema；
* examples；
* permissions；
* recommended models；
* route policy；
* minimum runner version；
* changelog。

当前代码中的 `SkillVersions` 已经具备 Skill Contract 的雏形，后续应把它升级为更明确的“能力契约”。

---

### 3.3 Skill Passport

Skill Passport 是每个 Skill 的可信档案。

它回答：

> 这个 Skill 靠不靠谱？

Passport 应包括：

| 维度   | 内容                  |
| ---- | ------------------- |
| 身份   | Skill ID、作者、版本、签名状态 |
| 能力   | 输入、输出、适用任务、示例       |
| 兼容   | 支持哪些模型、哪些模型需要适配器    |
| 可靠性  | 成功率、格式通过率、延迟、成本     |
| 安全   | 权限声明、敏感风险、审核状态      |
| 失败   | 已知失败类型、修复建议         |
| 证据   | 最近验证时间、样本量、来源权重     |
| 企业治理 | 是否被某企业批准、是否被限制、是否废弃 |

Passport 不是营销页，而是可信对象。

---

### 3.4 Model Profile

Model Profile 是模型画像。

它不只是价格表，而是模型行为档案。

示例字段：

* provider；
* model name；
* model version；
* context length；
* structured output 能力；
* tool use 能力；
* 中文风格能力；
* JSON 稳定性；
* 长输出稳定性；
* 常见失败类型；
* 价格；
* 隐私与区域特性；
* 是否被平台允许代付。

Model Profile 的目标是：

> 判断一个 Skill 在某个模型上为什么能跑、为什么不能跑、怎么适配。

---

### 3.5 Adapter Profile

Adapter Profile 是 Skill × Model 的适配补丁。

它解决：

> 这个 Skill 在某个模型上跑不好，应该怎么修？

示例：

```text
Skill：小红书标题生成器
Model：qwen-plus

问题：
JSON 漏字段、输出解释过多

Adapter：
- system prompt 增强“只输出 JSON”
- temperature 降到 0.4
- max_tokens 提高到 1200
- 失败后执行 JSON repair
- 输出字段缺失时自动重试一次
```

这是衡术长期最重要的护城河之一。

别人可以复制 Skill，但很难复制长期积累的：

> Skill × Model × Failure × Fix

---

### 3.6 Compat Test Case

Compat Test Case 是兼容测试样例。

每个重要 Skill 都应该有测试样例：

* 普通输入；
* 边界输入；
* 长文本输入；
* 容易跑偏输入；
* 结构化输出压力测试；
* 敏感场景防误用测试。

它的作用：

> 让 Skill 的兼容性不是主观评价，而是可重复验证。

---

### 3.7 Failure Case

Failure Case 是失败知识库的一条记录。

它不是简单记录“失败了”，而是记录：

* 哪个 Skill；
* 哪个版本；
* 哪个模型；
* 什么输入规模；
* 什么错误类型；
* 根因是什么；
* 怎么修；
* 修复后提升多少。

失败知识库是比成功率更有价值的数据资产。

---

### 3.8 Enterprise Registry

Enterprise Registry 是企业 AI Skill 资产管理层。

它包括：

* 私有 Skill 库；
* 企业批准状态；
* 模型白名单；
* 权限策略；
* 审计日志；
* 负责人；
* 版本锁定；
* 废弃与回滚；
* 企业私有评测结果。

长期商业化应主要依赖企业 Registry，而不是单纯模型调用 margin。

---

## 4. 当前代码底座映射

### 4.1 已落地能力

| 能力                      | 当前状态 | 代码基础 / 入口 |
| ----------------------- | ---- | ------------- |
| Skill 内容管理              | 已有   | `Skills`、`SkillVersions` |
| Skill Contract          | 已有   | `SkillVersions.contractHash`、`contractStatus`、`src/lib/skillContract.ts`、`/v1/skills/[slug]/contract` |
| Manifest 快照             | 已有   | `SkillArtifacts`，manifest checksum + ed25519 + `/v1/keys` |
| 本地 Runner               | 已有   | `runner/hengshu.mjs`，安装、本地运行、验签、回流 |
| 私人运行台账                 | 已有   | `SkillRuns` 输入/输出加密、`/v1/runs` 私人台账按模型版本筛选导出、`/v1/runs/[id]/rerun` 换模型/版本重跑 |
| 兼容报告 / 活体数据             | 已有   | `CompatReports`，时间衰减 + 来源权重聚合，按 `modelProfile` / modelVersion 分组，并展示有效样本 |
| Skill Passport          | 已有   | `SkillPassports`、`passportRefresh`、详情页 Passport 区块、公开 Passport API；API 返回带 evidenceHash 的黄金样例基准摘要和可信兼容运行计数；`/v1/skills/[slug]/certificate` 输出绑定 Contract 摘要和可信兼容运行计数的达标证书，`/v1/certificates/verify` 公开验签并返回绑定 Contract/Passport/基准摘要 |
| ModelProfile            | 已有   | `ModelProfiles`、`refresh-model-profiles`、`/v1/model-profiles`、`driftHistory` 漂移曲线、有效样本与来源权重 |
| CompatTestCase          | 已有   | `CompatTestCases`，benchmark 优先读取测试用例；黄金样例支持 requiredOutputPaths / expectedTextIncludes 逐条打分 |
| FailureCase             | 已有   | `FailureCases`、`cluster-failures`、`/failures` 失败知识库 |
| AdapterProfile          | 已有   | `AdapterProfiles`、运行时自动应用、lift 统计、FailureCase 生成草稿 |
| EvidenceSnapshot / 外锚   | 已有   | `EvidenceSnapshots`、`/v1/evidence/verify`、`/v1/anchors/verify`、证据 JSONL 外锚导出、manifest 自签名、第三方发布/时间戳声明与校验、assurance.level 可信等级 |
| Enterprise Registry     | 已有   | `Organizations`、`OrganizationMembers`、`EnterpriseRegistries`、审批/成员/审计 API、身份策略配置 |
| 企业运行授权与审计              | 已有   | `/v1/skills/[slug]/run`、`compare`、`/v1/enterprise/audit/export` |
| AI 上传审核                 | 已有   | 规则扫描 + AI 合规审核员 + 人工兜底 |
| Import Adapters         | 已有   | GitHub README / Claude Skill `SKILL.md` / GPTs 配置可导入为待审 Imported Skill；`worker:import-skill-sources` / `worker:sync-skill-sources` 支持来源清单批量导入、稳定幂等键、内容 hash、`--sync` 新版本同步与变更差分，可直接挂 cron |
| 贡献与 credit 台账           | 已有骨架 | `ContributionLogs`、`CreditLogs` |
| New API 联动              | 已有骨架 | `newapiAdmin`、`DeploymentSettings`，作为后台履约能力 |

### 4.2 半落地能力

| 能力             | 当前状态              | 需要补齐 |
| -------------- | ----------------- | ------ |
| SSO / SCIM      | 配置校验 + 最小 provision | `Organizations.identityPolicy`、`/v1/enterprise/identity`、`/v1/enterprise/scim/users`、控制台身份策略面板已承接 domainAllowlist、requireSso、OIDC、SCIM 配置；保存时校验 HTTPS URL、OIDC 必填项与 tokenDigest；添加 active 成员时已执行域名/SSO 策略；SCIM 兼容 `userName`/`emails`/`roles` payload、`userName/email/emails.value eq` filter 和 PATCH Operations，可按 email 查询、创建/绑定、停用成员，并支持标准 ListResponse 成员列表 |

### 4.3 暂不做 / 后续能力

| 能力       | 状态 | 后续 |
| -------- | -- | ---- |
| 私有部署     | 已有 readiness 检查 | `worker:preflight-private` 支持 NAS/私有部署前检查默认密钥、数据库密码、URL 同源、端口、备份与媒体持久化提示；公网生产仍走 `worker:preflight-production` |
| 第三方可信声明 / 可信网络 | 分数/证据外锚 manifest 已支持站点 ed25519 自签和公开 API 校验；证据外锚支持第三方发布 / 时间戳声明；部署设置可配置可信发布目标，`/v1/anchors/verify` 可校验命中状态、外部时间戳 receiptHash，并输出 `assurance.level` 可信等级；生产预检会阻断非法可信发布目标 | 接真实第三方签章 / 时间戳服务 |
| SSO / SCIM | 已有配置校验和最小 SCIM provision | 接真实登录连接器 |

---

## 5. 护城河模型

### 5.1 错误护城河

以下不是长期护城河：

| 方向             | 问题                                |
| -------------- | --------------------------------- |
| Skill 数量       | 容易复制、质量参差、容易变下载站                  |
| 下载量            | 可刷、弱信号、不能代表稳定性                    |
| 单纯兼容数据         | 会腐坏、样本不足、容易被爬                     |
| 省钱路由           | 容易被 OpenRouter / New API / 模型网关吃掉 |
| New API margin | 价格战明显，利润薄，不适合作为主壁垒                |
| 排行榜            | 容易诱导虚假繁荣                          |
| 社区积分           | 没有核心资产时会变成运营游戏                    |

### 5.2 正确护城河

衡术真正应建立五层护城河：它们不靠“攒一张静态兼容表”，而是长在真实运行回流、私人台账、失败修复和可验签证据链上。

#### 第一层：活体兼容数据

兼容数据不是一次性榜单，而是随真实运行持续更新的活体画像。

现状：

* `CompatReports` 聚合真实运行回流；
* `compat.ts` 已按模型与模型版本聚合，带时间衰减、来源权重和置信度；
* `ModelProfiles` 展示有效样本、来源权重和漂移摘要。

目标态：

* 以 `(modelName, modelVersion)` 为主键持续记录跨版本漂移；
* 把模型升级、退化、格式偏移沉淀成可追踪曲线。

价值：

* 竞品可以爬走今天的结论，但爬不走连续回流和跨版本漂移；
* 数据会腐坏这件事本身会变成壁垒，因为只有持续运行的一方能刷新它；
* 用户看到的是“我的模型现在能不能跑”，不是泛泛排行榜。

---

#### 第二层：数据驱动的成本优化路由

省钱路由本身不是护城河，真实回流驱动的自动降本才是。

现状：

* `routePolicy.dataDriven` 可由真实运行回流重算；
* `selectModel` 优先结合个人历史和全站兼容数据；
* 控制台展示运行后的成本、延迟和“省钱回执”。

价值：

* 抄一张“推荐模型表”很容易；
* 抄“基于真实 BYOK / Runner / 平台代付运行自动刷新”的路由很难；
* 它让用户在不换平台、不重试错的情况下获得更低成本。

---

#### 第三层：私人运行台账

私人台账记录用户自己的运行、输入输出、模型、成本、延迟和失败结果。

现状：

* `SkillRuns` 保存用户运行记录，并对输入输出做权限与加密边界；
* `/v1/runs`、控制台运行台账和换模型重跑已经形成闭环；
* 重跑结果会继续回流到兼容证据和失败知识。

价值：

* 这是用户自己的数据，不是公共平均分；
* 使用越多，迁移成本越高；
* 它堵住“看完 Skill 后直接去网关裸跑”的流失点。

---

#### 第四层：任务可靠性与失败库

失败库记录“怎么坏、为什么坏、怎么修、修完有没有提升”。

现状：

* `FailureCases` 从真实失败中聚类；
* `/failures` 提供脱敏失败知识库；
* `AdapterProfiles` 可从失败案例生成草稿，并统计修复 lift。

目标态：

* 聚合键下移到 `skill × 输入档 × errorType × modelVersion`；
* 对代际回归、格式偏移和适配失效发出告警。

价值：

* 模型能力趋同后，“任务怎么稳定交付”反而更值钱；
* 失败数据来自真实任务和真实修复，比成功率更难复制；
* Adapter 让失败库不只是展示问题，而是能进入运行修复。

---

#### 第五层：可验签基准与企业可信网络

可信不是平台自说自话，而是 Passport、证书、证据快照和外锚共同构成的可验签证据链。

现状：

* manifest / Passport / certificate / EvidenceSnapshot 已有 ed25519 签名和公开验签入口；
* `/verify`、`/v1/evidence/verify`、`/v1/certificates/verify`、`/v1/anchors/verify` 提供验证能力；
* Enterprise Registry 把审批、模型白名单、审计、企业失败库和组织内 Passport 串起来。

目标态：

* 黄金样例逐条打分；
* 第三方时间戳 / 签章 / 可信发布网络接入；
* 企业可把内部批准与外部证据合并成自己的 AI Skill Registry。

价值：

* 网关自评自证存在利益冲突，中立可验签是结构性优势；
* 企业买的不是“更多 Skill”，而是可审批、可追责、可回滚、可换模型的治理能力；
* 证据链越长，创作者、企业和平台的迁移成本越高。

---

## 6. 产品策略

### 6.1 前台主叙事

前台不再主打：

> 哪个模型最省。

前台主打：

> 这个 Skill 是否可信，是否适配你的模型。

Skill 详情页第一屏应展示：

```text
Skill Passport
兼容模型
当前模型是否可用
是否需要 Adapter
最近验证时间
已知失败
安全权限
企业是否批准
```

---

### 6.2 后台能力

后台仍保留：

* 路由；
* 省钱；
* 模型对比；
* New API 子令牌；
* credit 预检；
* 成本记录；
* benchmark worker。

但这些是运行优化能力，不是主叙事。

---

### 6.3 Skill 市场的定位

Skill 市场只是入口。

真正终局是：

```text
Skill 市场
→ Passport
→ Compatibility
→ Adapter
→ Failure Intelligence
→ Enterprise Registry
```

---

### 6.4 本地 Runner 的定位

本地 Runner 是信任入口。

它代表：

* 用户可控；
* 企业可控；
* 数据可不出本地；
* 模型可自选；
* 兼容数据可选择性回流；
* manifest 可签名验证。

本地 Runner 不是附属工具，而是衡术可信体系的关键组成。

---

## 7. 上架与审核规则

### 7.1 Skill 分级

| 等级              | 定义                                         | 是否可自动上架                     |
| --------------- | ------------------------------------------ | --------------------------- |
| Verified Skill  | 有合法 manifest、明确 contract、低风险权限、通过规则与 AI 审核 | 可以                          |
| Imported Skill  | 没有 manifest，但 README / 简介可解析               | 不自动上架，进入 pending / unlisted |
| High-risk Skill | 涉及网络、文件读写、Shell、脚本、密钥、外部服务                 | 不自动上架，必须人工审核                |
| Rejected Skill  | 明显恶意、诈骗、窃取凭据、违法违规                          | 拒绝                          |

### 7.2 Manifest 红线

无 manifest 的 Skill 可以被收录、解析、生成预览，但不得获得 Verified 自动上架。

Verified 必须满足：

* 有 `hengshu.skill.yaml/yml`；
* `schema_version` 合法；
* 有 `prompt.user_template`；
* 有 input schema；
* 有 output schema 或明确 text output；
* 权限声明完整；
* 无高风险文件；
* 无敏感信息诱导；
* 通过 AI + 规则审核；
* 可以生成 Skill Passport。

### 7.3 AI 可以自动完成的工作

AI 可以做：

* 解析 README；
* 生成 Skill 摘要；
* 识别用途；
* 提取输入输出；
* 生成示例；
* 初步判断风险；
* 生成兼容测试样例；
* 生成 Passport 初稿；
* 生成 Adapter 建议；
* 聚类失败原因；
* 输出修复建议。

### 7.4 规则系统必须完成的工作

规则系统必须做：

* manifest 格式校验；
* checksum；
* 签名；
* 权限声明校验；
* 敏感文件扫描；
* 密钥脱敏；
* 高风险路径拦截；
* 执行权限拦截；
* 企业策略校验；
* 自动上架资格判断。

### 7.5 人工必须介入的工作

人工介入：

* 高风险 Skill 审核；
* 网络 / 文件 / Shell / 脚本 Skill；
* 企业上架审批；
* 安全事件处理；
* 争议处理；
* 高价值官方 Skill 维护；
* 高影响 FailureCase 归因确认；
* 核心 Adapter 质量确认。

---

## 8. 数据与隐私边界

### 8.1 数据分层

| 数据                  | 默认处理              |
| ------------------- | ----------------- |
| Skill 元数据           | 可公开               |
| Manifest / Passport | 可公开或组织内公开         |
| 用户输入输出              | 默认私有，加密存储         |
| 本地 Runner 结果        | 默认不回传原文           |
| CompatReports       | 不含原始输入输出          |
| FailureCase         | 只保留脱敏症状、错误类型、修复建议 |
| 企业私有 Skill          | 组织内可见             |
| 企业运行记录              | 组织审计范围内可见         |

### 8.2 原则

* 原始输入输出不进入公开聚合；
* 兼容报告只存可聚合指标；
* Passport 只展示聚合证据；
* 失败知识库只展示脱敏信息；
* 企业数据必须组织隔离；
* BYOK 和 Local-first 是信任优势；
* 平台代付不能凌驾于隐私边界之上。

---

## 9. 商业模式

### 9.1 免费层

面向个人和开源用户：

* 浏览公开 Skill；
* 查看公开 Passport；
* 安装 Runner；
* 下载公开 manifest；
* 提交有限兼容报告；
* 在线有限试用。

### 9.2 Pro 层

面向重度个人和创作者：

* 私有 Skill；
* 更长运行历史；
* 私人台账；
* 模型兼容分析；
* Adapter 建议；
* 失败诊断；
* 更多测试额度。

### 9.3 Team 层

面向小团队：

* 团队 Skill 库；
* 团队共享 Passport；
* 团队运行记录；
* 简单审批；
* 成员权限；
* 团队模型配置。

### 9.4 Enterprise 层

面向企业：

* 私有 Enterprise Registry；
* SSO / SCIM；
* 审批流程；
* 审计导出；
* 模型白名单；
* 策略包；
* 私有 benchmark；
* 私有 FailureCase；
* 私有 Adapter；
* 数据隔离；
* 专属部署或私有云。

### 9.5 New API margin 的定位

New API / 模型网关联动是履约能力，不是主商业模式。

它用于：

* 在线试用；
* 平台代付；
* credit 消费；
* benchmark；
* 低门槛体验；
* 运营闭环。

但长期收入不应主要依赖模型调用 margin。

---

## 10. 经济系统定位

### 10.1 credit

credit 是算力燃料。

用途：

* 在线运行；
* 平台代付；
* benchmark；
* 企业测试额度。

约束：

* 不反向提现；
* 不参与随机收益；
* 必须有真实成本控制；
* 真实网关未校准前不得开放高风险兑换。

### 10.2 贡献值（原“术值”）

贡献值是贡献信誉，不是金融资产。

可用于：

* 创作者信誉；
* 贡献记录；
* 兑换有限 credit；
* 排名辅助；
* 企业参考信号。

贡献值不应成为平台主线。

### 10.3 兑换池

兑换池必须后置。

只有在满足以下条件后开启：

* New API 真实用量校准完成；
* 本地 credit 账本与网关 quota 对账稳定；
* 毛利来源可信；
* 生产备份与审计完成；
* 兑换逻辑经测试锁死；
* 法务风险确认。

---

## 11. 路线图

### 阶段 0：定位收束与文档重构

目标：停止旧叙事漂移。

任务：

* 重写总纲为 v2；
* README 首段同步新定位；
* 首页文案改成“可信与兼容”；
* 成本优化路由降级为后台能力；
* New API margin 降级为可选履约；
* 贡献值经济后置。

验收：

* 所有文档不再把“帮用户换模型”作为主卖点；
* 所有文档统一表达“让 Skill 适配用户已有模型”。

---

### 阶段 1：Skill Passport

目标：让每个 Skill 有可信档案。

任务：

* 新增 `SkillPassports`；
* 从现有 Skills / SkillVersions / SkillRuns / CompatReports 回填；
* Skill 详情页增加 Passport tab；
* 生成 trust score 初版；
* 增加 Passport 更新时间与证据来源；
* 明确 Verified / Imported / High-risk 分级。

验收：

* 每个 published Skill 都能生成 Passport；
* Skill 详情页第一屏能展示可信状态；
* 无 manifest Skill 不再自动获得 Verified。

---

### 阶段 2：Model Profile + Compatibility

目标：把模型从字符串升级为画像。

任务：

* 新增 `ModelProfiles`；
* 聚合当前 `modelName`；
* 增加 provider / version / structured output / price / known issues；
* 兼容矩阵从 `modelName` 升级到 `modelProfile`；
* 增加 freshness 过期机制。

验收：

* 用户能看到“我的模型是否支持这个 Skill”；
* 过期数据不再显示为强结论；
* 模型版本变化可被追踪。

---

### 阶段 3：Adapter Profile

目标：平台不只是评价兼容性，而是修复兼容性。

任务：

* 新增 `AdapterProfiles`；
* 为重点 Skill × Model 生成适配补丁；
* 支持 prompt patch；
* 支持 schema patch；
* 支持 decoding patch；
* 支持 retry / repair policy；
* 记录 adapter lift。

验收：

* 至少 20 个官方 Skill 有 2 个以上模型适配器；
* 能展示“适配前 / 适配后”成功率或格式通过率变化；
* 用户运行时可自动应用 Adapter。

---

### 阶段 4：Failure Intelligence

目标：建立失败知识库。

任务：

* 新增 `FailureCases`；
* 从 `SkillRuns.errorCode`、`CompatReports.errorType` 聚类；
* 记录 root cause；
* 记录修复策略；
* 记录复验结果；
* 提供相似失败检索。

验收：

* 每类常见失败都有脱敏案例；
* Adapter 生成能引用 FailureCase；
* 失败页面不泄露原始输入输出。

---

### 阶段 5：Enterprise Registry

目标：商业化主线。

任务：

* 新增组织 / 团队模型；
* 新增 Enterprise Registry；
* 新增审批状态；
* 新增策略包；
* 新增企业模型白名单；
* 新增审计导出；
* 新增私有 Passport；
* 新增私有 FailureCase。

验收：

* 企业能导入并批准 Skill；
* 员工只能使用批准 Skill；
* 所有运行有审计；
* Skill 可禁用、回滚、限定模型。

---

## 12. 开发优先级

### P0：已完成基线

1. `docs/衡术-总纲.md` 已切到 v2 结构；
2. README 首段已同步新定位；
3. 无 manifest 的 Skill 不再自动 Verified 上架；
4. 已新增 `SkillPassports` / `ModelProfiles` / `AdapterProfiles` / `FailureCases` / `CompatTestCases`；
5. Skill 详情页已展示 Passport 与证据摘要；
6. 已提供 `worker:backfill-passports` 生成现有 Skill 的 Passport 初稿；
7. 已补 Passport / manifest / 签名确定性测试。

### P1：已完成短期强化

1. benchmark worker 已支持 CompatTestCase，并能对黄金样例逐条打分；
2. ModelProfile 已支持 freshness、modelVersion 与 `driftHistory` 漂移曲线；
3. FailureCase 已可从真实回流聚类并写证据快照；
4. Adapter lift 已可统计，运行时可自动应用 active Adapter；
5. 失败库已支持从 FailureCase 生成待审核 Adapter 草稿；
6. Enterprise Registry 已有组织、成员、审批、模型白名单、基础策略包、控制台策略编辑、运行授权、审计导出、组织内 Passport + 达标证书 API 和企业失败知识库；
7. EvidenceSnapshot 已支持公开验签、JSONL 外锚 manifest 自签/校验、第三方发布 / 时间戳声明字段，以及外锚可信等级输出。

### P2：后续推进

1. SSO / SCIM 从配置校验升级为真实 OIDC/SAML 登录；SCIM 继续补更复杂的标准过滤细节；
2. 私有部署；
3. 第三方可信声明接入真实外部签章 / 时间戳服务；
4. 可信网络；
5. GitHub / Claude Skills / GPTs import adapters 已支持批量来源导入、内容 hash、同步 worker 和变更差分；可直接交给 cron 定时执行。

### 暂缓

1. 复杂贡献值兑换；
2. 充值返利宝箱；
3. PT 身份系统；
4. 论坛 / 广场；
5. 中转联盟；
6. 过度复杂排行榜；
7. 大规模社区运营。

---

## 13. Codex 执行清单

### 13.1 文档任务

```text
已完成：

1. docs/衡术-总纲.md 已替换为总纲 v2。
2. README 首段已从“哪个模型真省真稳”改为“让 Skill 适配用户已有模型、网关、本地 Runner 和企业环境”。
3. docs/ARCHITECTURE_V2.md 已新增，用于映射现有代码到 v2 对象，并标明已落地 / 半落地 / 后续能力。
```

### 13.2 Schema 任务

```text
已新增并注册到 src/payload.config.ts：

- SkillPassports
- ModelProfiles
- AdapterProfiles
- FailureCases
- CompatTestCases
- EvidenceSnapshots
- Organizations
- OrganizationMembers
- EnterpriseRegistries
- EnterpriseAuditLogs
```

### 13.3 API 任务

```text
已接入 /v1 API：

GET  /v1/skills  # 摘要含顶层 trustedCompatibleRunCount
GET  /v1/skills/[slug]/contract
GET  /v1/skills/[slug]/passport
GET  /v1/model-profiles  # 支持 modelName/modelVersion/provider/status 过滤
GET  /v1/evidence/verify
POST /v1/anchors/verify
POST /v1/certificates/verify
GET  /v1/failures  # 支持 errorType/modelName/status/skillId/profileKey/inputBucket/source 过滤
GET  /v1/adapters  # 支持 skillId/modelName/modelVersion/failureType/failureId/modelProfile/status 过滤
POST /v1/failures/[id]/adapter
POST /v1/enterprise/registry
POST /v1/enterprise/members
GET  /v1/enterprise/registry/[id]/passport  # 含组织内 Passport、治理状态、证据验签摘要和绑定 Contract 的达标证书
GET  /v1/enterprise/audit/export
GET  /v1/enterprise/failures
POST /v1/enterprise/identity
POST/GET/PATCH/DELETE /v1/enterprise/scim/users  # SCIM provision / ListResponse / active PATCH / 停用
GET  /v1/runs  # 支持 skillId/model/routeMode/success/formatValid/trustedCompatible/rerunOf 过滤
POST /v1/runs/[id]/rerun  # 支持 modelProvider/modelVersion
```

### 13.4 Worker 任务

```text
已新增 worker：

worker:backfill-passports
worker:refresh-model-profiles
worker:backfill-compat-model-profiles
worker:cluster-failures
worker:export-evidence-anchors
worker:verify-evidence-anchors
worker:import-skill-sources
worker:preflight-private
```

### 13.5 UI 任务

```text
已接入 UI：

- Skill 详情页 Passport / 证据摘要 / 兼容矩阵，并从每个模型跳转模型画像与该 Skill×模型失败库
- /skills/[slug]/run 在线试跑前展示 Passport 可信分，并提供 Passport、Contract、达标证书和证据验签入口
- /models 模型画像、回归告警、漂移摘要、模型名/Provider/状态筛选，并可跳转模型画像 API 与该模型失败库
- /failures 失败知识库、多维筛选、失败/Adapter 证据验签、Adapter 建议，并展示“发现失败模式 → 生成 Adapter 草稿 → 复验 lift”的闭环
- /rank 可信发现榜，展示可信分、成功率、运行样本、Passport 可信分和公开证据入口
- /bounties 求术悬赏引导用户把需求变成可版本化、可签名、可进入 Passport 闭环的 Skill
- /console 个人控制台概览突出私人运行台账的总运行、成功、格式有效和换模型重跑
- /console/runs 私人运行台账、页面多维筛选、可信兼容筛选、账本/输入输出按筛选导出、推荐/自定义模型重跑、重跑成本/延迟对比；/v1/runs 支持多维筛选导出
- /console/enterprise 企业策略包与身份策略配置，并展示“批准 Skill → 绑定策略 → 留审计 → 查失败库”的治理闭环
- /docs 用户功能说明首页按找 Skill、本地 Runner、发布、私人台账、公开验签、企业 Registry 六条路径直达关键入口
- /verify 公开解释分数快照、达标证书、证据快照、外锚包四类证据链；提供验签列表、达标证书在线验签表单（支持 certificateUrl 自动载入并展示绑定 Contract）、证据快照 targetSummary 在线验签表单、外锚包在线校验表单和外锚可信等级展示
- 首页“先跑必备 Skill”新手入口；首页 SkillCard 展示 Passport 可信分；首页引导从已有运行证据的 Skill fork 成新版本；/skills?essential=1 必备筛选；/skills 必备 Skill Starter Pack，并给出看 Passport、默认试跑、回台账重跑的新手路径；市场列表展示 Passport 可信分和公开证据入口
```

---

## 14. 最终北极星指标

旧指标：

```text
下载量
Skill 数量
排行榜
省钱金额
术值增长
```

新指标：

```text
可信兼容运行数 Trusted Compatible Runs
```

定义：

> 在有当前 Passport、明确 ModelProfile、有效兼容证据或 Adapter 支撑下完成的 Skill 运行。

可信分会把可信兼容运行计数作为证据强度因子，但采用对数饱和，避免重新变成刷调用量排行榜。

辅助指标：

| 指标                           | 说明                           |
| ---------------------------- | ---------------------------- |
| Passport 覆盖率                 | 有 Passport 的 active Skill 占比 |
| Model 覆盖率                    | 有 ModelProfile 的模型占比         |
| Adapter lift                 | 适配后成功率 / 格式率提升               |
| Failure reuse rate           | 失败能匹配已知 FailureCase 的比例      |
| Evidence freshness           | 可信结论是否有近期证据                  |
| Enterprise Registry Adoption | 企业批准 Skill 数和注册表运行量          |
| Local Runner Active Installs | 活跃本地安装数                      |
| Verified Skill Ratio         | Verified Skill 占发布 Skill 的比例 |

---

## 15. 最终判断

衡术 v1 已经不是空壳。

当前代码已经具备：

* Skill 版本；
* manifest；
* 签名；
* Runner；
* 运行台账；
* 兼容报告；
* 经济台账；
* 审计；
* AI 审核；
* New API 骨架。

但 v1 的文档和叙事仍然容易把项目带向：

> Skill 市场 + 省钱路由 + 中转履约。

v2 必须重新收束为：

> AI Skill 的可信与兼容控制平面。

最终目标：

```text
一个 Skill
→ 有 Passport
→ 有签名版本
→ 有兼容模型
→ 有适配补丁
→ 有失败记录
→ 有测试证据
→ 可进入企业注册表
→ 可被长期治理和复用
```

这才是衡术最值得长期投入的方向。
