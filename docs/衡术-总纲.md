# 衡术 Hengshu — 总纲（唯一开发依据）

> 状态：**开发蓝图 · 单一事实来源** · 更新：2026-07-02
> 本文整合并取代此前 4 份规划稿（产品方向与护城河 / 成品化技术清单 / 生产部署清单 / 联动经济方案），后续开发优化**只依据本文**。
> 历史参考（不在本文范围、保留原文）：`hengshu_prd_v0.2.1.md`(原始 PRD) · `体验手册.md` · `PROGRESS.md`(功能进度基线)。

---

## 1. 产品定位

**衡术是一个中心化运营的 AI Skill 平台，"决策层 + 履约层"一体机**：
- **决策层**：中立评测 + 省钱路由——用真实运行数据告诉用户"这个 Skill 在哪个模型上真省真稳"，并自动帮你选当前实测最省够用的模型。
- **履约层**：作者自有的 **New API 中转**——模型调用真正发生的地方，平台从 margin 赚钱。
- **算力 = 用户自带 Key（BYOK），默认走自己中转**；平台不承担现金算力成本，从中转 margin 变现。
- **公网多用户中心化**（非自部署）；单一中心 → 兼容报告/术值榜/求术全站聚合，PT 网络效应成立，信任模型为中央签名。

---

## 2. 护城河与竞争力（战略核心）

**诚实前提**：单纯"攒兼容数据"不是护城河——结论低密度可爬、评分不奖励样本量、网关在数据上游、数据会腐坏、看一眼就走。**护城河必须挪到"长在真实 BYOK 流量上、且创造切换成本"的地方。**

**加固后的 5 层护城河**（全部复用已有代码资产）：

| 层 | 是什么 | 为何抄不走 |
|---|---|---|
| **0 活体数据** | `compat.ts` 聚合层按 (modelName,modelVersion) 二级分组 + `recencyScore` 衰减(halfLife=30天) + 置信区间；招牌只用当前窗口 | 竞品抓走"今天"，抓不走连续过去与跨版本漂移曲线；腐坏反成壁垒 |
| **1 省钱路由** ⭐ | `recomputeLocalScore` 回写 `routePolicy`，绑 BYOK 后自动选实测最省够用模型、漂移自动换 + console"省钱回执" | 抄"看表"易，抄"真实回流驱动的自动降本路由"须先有等量真实运行 |
| **2 私人台账** ⭐ | 激活 `SkillRuns`（已存 input/output）做"我的运行台账 + 换模型一键重跑" | 用户**自己的**数据，非公共平均；迁移成本随使用线性上升；堵死 BYOK"查完去网关直连"漏点 |
| **3 任务可靠性** | 聚合键从 modelName 下移到 profile(skill×输入档×errorType)；代际回归告警 | 模型趋同后不消失反更值钱（prompt 工程质量差异不趋同） |
| **4 可验签基准** | verified + ed25519 升级为第三方达标证书；黄金样例逐条打分 | 网关自评自证有利益冲突，中立性结构上是你的 |

**⭐ 最锋利竞争力（复购与切换成本唯一来源）= 第1层×第2层**：
> **"绑你的 Key 后，用你自己历史 prompt 的真实回流，持续替你选最省的模型跑、漂移自动换路由、并累计告诉你这月省了多少钱。"**
> 个人化 + 每周涨可推送 + 离开就丢台账——省的钱沉淀在平台账本而非用户脑子里。

---

## 3. 经济模型（变现，且反哺护城河）

### 3.1 三币单向漏斗
```
人民币 ──充值──▶ credit(算力燃料) ──跑模型──▶ 蒸发
贡献行为 ──挣──▶ 术值 ──受控兑换──▶ credit
                     术值/credit 永不反向变现金 ❌（保命红线：防资金盘/赌博）
```

| 币 | 角色 | 护栏 |
|---|---|---|
| CNY | 唯一入金口（New API 兑换码充值） | 衡术不碰现金；无任何→CNY 出口，代码硬禁止 |
| credit | 唯一花算力的币(1credit=¥0.01零售) | 物理映射为受限子令牌（低价分组+配额熔断+TTL）；free 档不产术值/不可兑换转让→白嫖上限=进货成本，套现≈0 |
| 术值 | 贡献信誉分（不可买只能挣） | 高价值行为**被采纳才发分**；不可转赠；永不进随机池 |

**核心不变量（结构性防亏）**：术值兑换池 = **α × 当月已实现毛利**(α=0.30) → 先赚到才有得兑，**永不亏 margin**，最坏少赚 30%。

### 3.2 抽奖的最终结论：**不做纯抽奖，做"满额返利宝箱"**
纯抽奖/开箱踩两条红线（反不正当竞争法有奖销售 + 变相赌博）。改：**充值满额 100% 必得**确定返利(6% 公示)，**随机只决定返利形态**（都是额度券，用户永不空手）——类"第二杯半价+随机送小料"。术值绝不进随机池。⚠️ 上线前走一次律师。

### 3.3 中立与变现共存：四面墙（护城河不被变现稀释）
1. **数学隔离**：`modelrank.ts` 排名函数入参**无 margin/isOurs 字段** + CI `grep` 断言（人事后也改不动）。
2. **记账隔离**：benchmark 按成本价记账(margin=0)；履约 SkillRuns 不进榜分。
3. **成本真值隔离**：用公开官方报价快照(`ModelPriceSnapshot`)非平台可编辑价；双列"官方价 vs 我到手价"，更贵必红标"BYOK更省"。
4. **履约隔离**：run 响应强制回传 `cheaperViaByok`，更省走 BYOK 不得隐藏。

### 3.4 New API 对接
1 个 New API 账号 = 全体用户，靠**子令牌**隔离（`token_name="hs_<userId>"`）。注册发受限子Key(60秒真跑)；术值换额度=后端原子事务(先扣术值→`PUT /api/token`加配额→失败回滚)；cron 拉 `/api/log` 用量真值回流喂护城河(剥离明文)。

---

## 4. 现状与差距

### ✅ 已建成（S1-S7 + 信任模型，代码核实真跑通）
下载→安装→离线运行(Runner CLI)→兼容回流+LocalScore→术值/贡献2.0→求术悬赏→manifest ed25519 签名；前台首页/市场/详情/排行/悬赏/控制台；评论收藏闭环。
**本会话已修**：求术钱路原子化+幂等 · Media 本地卷 · DB idle 超时。

### ❌ 关键差距
| 缺口 | 严重度 |
|---|---|
| **创作者供给端断路**（creator 进不了后台、前台无发布界面 → 没有内容来源） | 🔴🔴 命脉 |
| **🚨 register 邀请 +5 走无限回退分支**（自建号互邀可无限印术值，已上线出血） | 🔴 立即修 |
| 数据一致性（缺唯一约束致 favoriteCount 虚高/删用户留孤儿/指标丢更新不可重算/余额竞态） | 🔴 |
| 通知系统 / 举报入口 / 用户设置(自助改Key) 缺失 | 🔴 |
| 测试与 CI ≈ 0 | 🔴 |
| 前端缺移动导航/错误加载边界/SEO/分页/Toast；README 严重过期 | 🟡 |
| `compare` 的 `skipAggregate:true` 丢弃线上评测信号（关着护城河水龙头） | 🔴 一行改 |

---

## 5. 统一落地路线（唯一开发顺序）

> 合并了原部署/成品/护城河/经济四份的所有优先级。按阶段推进，每阶段独立冒烟。

### 🚨 阶段 0 · 立即止血 + 护城河地基（本周，多为 0.5-2 天/项，不依赖外部决策）
1. **修 register 术值漏洞** ✅限速已做(2026-07-02，对抗审查确认正确无回归)：register 改为依赖规则表(不传 points)；invite/skill_version_update 加 `dailyLimit=3`。⚠️ **残余(未根除)**：① 自邀刷分——invitee≠inviter 使 `selfActionExcluded` 失效，需 #6 设备/IP 去重才能堵死。
1b. **修 skill_published(+50) 反复发布刷分** ✅已修(2026-07-02，对抗审查通过)：afterChange 按 Skill 幂等(查 contribution-logs 已有 skill_published 则不再发)，反复 published↔非published 不再重复奖励。残余[低]：并发双发竞态(check-then-act 非原子)。注：全局 `(relatedSkill,actionType)` 唯一约束会误伤 skill_run/favorited 等每 Skill 多条日志的行为，**不可行**；正解是 partial unique(仅 WHERE actionType 为一次性行为)或保持现有应用层幂等(已足够，竞态极低)。
2. **`compat.ts` 聚合层重写** ✅已做(2026-07-02，对抗审查通过)：时间指数衰减加权(halfLife=30天，近期主导抗腐坏) + LocalScore 置信度衰减(有效样本<10 则按比例压分，杜绝 N=1 的 100% 噪声霸榜) + decayWeight 缺时间戳按 0 权重(防"无时间=最新"刷分)。**残余**：① confidence 对长期无回访的旧优质 Skill 会指数压到 0(与"无数据"不可区分)，考虑加地板或区分展示 ② limit:5000 全量+同步重算是性能定时炸弹，改时间窗(近180天)拉取+异步重算可一并治旧数据腐坏 ③ (modelName,modelVersion) 二级分组/漂移曲线留阶段4。
3. **水龙头·在线评测回流** ✅已做(2026-07-02，对抗审查通过)：在 `runSkill` 里为真实非 mock 调用回流 `source='online'` 兼容报告（不含原文，仅分档/成败/耗时），在线试用与多模型对比都喂护城河。**抗刷三重**：排除作者自跑 + 单用户对同一(skill,model)封顶3条 + 来源分级权重(verified=1/community=0.5/**online=0.3**)削弱刷分投毒杠杆。**残余**：作者用小号仍可绕自跑排除(需 #6 设备/IP 去重根治)；localScore 缓存不在热路径刷新(靠 report/worker 收敛，反而更安全)。
4. **最小样本门槛** ✅已做(2026-07-02)：单模型报告<5 则前端显示"战绩积累中·已N次"(colSpan 跨三列)而非误导性百分比(`aggregateByModel` 返回 lowSample，详情页据此渲染)。
5. **数据一致性地基**：✅**批次A已做**(2026-07-02，对抗审查通过)——`Favorites(user,skill)`/`Reviews(user,skill,type)`/`SkillInstalls(user,skill,runner)` 复合唯一约束 + favorite/install 竞态兜底(回查确认再兜底) + `Categories`/`RunnerClients` beforeDelete 清孤儿(含 device-codes) + ReviewForm 唯一冲突友好提示。⚠️ **上线前必须先去重**：favorites/reviews/skill-installs 若有历史重复行，dev push 建唯一索引会失败（无 migrations），先跑去重 SQL。**残余**：① ✅ **Users 删除级联已做**(2026-07-02，对抗审查通过：阻断有作品/进行中悬赏者 + 删私有从属触发计数修正 + 解引用保留历史；补全 reports/skill-versions 遗漏外键；原子回滚。Payload postgres `_rels` cascade 特性已确认——遗漏字段是静默丢引用而非删不掉) ② `SkillInstalls.runner` 可空致 NULL 组唯一失效（当前调用链恒非空，潜在）③ 指标可从 SkillRuns 重算 ④ 余额行锁。
6. **反女巫地基** ✅ipHash 已做(2026-07-02，对抗审查通过)：Users 加 ipHash(HMAC不可逆/admin-only/hidden)；register 采集 IP(**从右往左取 XFF 防伪造**+`TRUSTED_PROXY_COUNT`) → 同 IP 24h 注册宽松频控(20，避 CGNAT 误伤，含容错) + 同 IP 自邀不发术值(根治小号刷邀请)。**残余**：① XFF 需生产反代 append 真实对端才可信(部署时配) ② deviceHash/前端指纹未做(能防同 IP 之外的多开) ③ ipHash 仅注册快照、跨网可绕 ④ PAYLOAD_SECRET 缺失回退固定盐(并入 env 校验项) ⑤ 同网真人互邀不计术值(需 FAQ 明示)。

### 阶段 1 · 供给命脉 + 护城河数据源（成败关键）
7. **前台"发布 Skill"表单** ✅已做(2026-07-02，对抗审查通过：安全/越权/原子/回归全过)：`POST /v1/skills`(登录即可，status 硬编码 pending、author 硬编码本人、白名单字段防注入)事务内建 Skill+首版本、版本 afterChange 自动设 currentVersion；前台 `SkillForm`+`/console/skills/new`+侧栏入口；单作者 pending 存量上限 20 防刷。**残余**：① ✅ "我的作品"页(A4，`/console/skills`)+ 草稿预览(A5，作者/审核员可在详情页预览自己未发布 Skill) 已做(2026-07-02，安全审查确认不泄露) ② 无幂等键，超时重试会生成不同 slug 的重复 Skill(低危，审核期人工去重)。
8. **发布即评测**：发布/fork 后自动跑一轮 compare 生成初始 LocalScore。
9. **5 种子标"可 fork 模板"** + 首页"从改一个现成的开始"。
10. **作者亲手灌 20-30 个满数据 Skill**（几十块 API 费，跨过飞轮静摩擦）。
11. **New API 子令牌联动**：封装管理 API（现 `newapi.ts` 只有 chatCompletion）；注册发受限子Key(60秒真跑)；cron 拉 `/api/log` 落评测真值表。
12. **中立四面墙**：`modelrank.ts`+CI断言 · `ModelPriceSnapshot` · benchmark margin=0 · run 加 `cheaperViaByok` · 双列价 · 中立看板。

### 阶段 2 · 变现闭环 + 最锋利竞争力
13. **术值换 credit 兑换池**：复用 Bounties 事务范式；动态率 R + α×M 池 + 三层上限 + 池空熔断。
14. **术值明码标价商城**：曝光置顶/徽章/verified插队/ratioScore特权，零随机。
15. **省钱路由落地**（护城河第1层）：`recomputeLocalScore` 回写 `routePolicy` + console"省钱回执"。
16. **私人运行台账**（护城河第2层）：SkillRuns 台账 + 换模型一键重跑；Favorites/Installs 升级为订阅告警。
17. **通知系统 + 用户设置(自助改模型Key) + 举报入口**。
18. **返利宝箱**（最后，先确定性形态，**需法务 review**）。

### 阶段 3 · 成品化（UX + 质量 + 上线）
19. **前端基建**：✅ 移动导航(汉堡菜单 `MobileNav`) · ✅ error/loading/not-found 边界(全站前台) · ✅ SEO(sitemap/robots + 详情页 generateMetadata，2026-07-02；⚠️用 NEXT_PUBLIC_SERVER_URL 受 build 期内联影响，待 SERVER_URL 去内联) · ⏳ 列表分页 · Toast/一键复制 · ✅ 移除登录页种子凭据(admin12345/WELCOME1，2026-07-02)。
20. **测试 + CI**：Vitest 地基 · 签名验签一致性测试 · 术值账本集成测试 · `.github/workflows`(typecheck+lint+test+build)。
21. **部署上线**：schema 迁移体系 · 密钥强随机 · HTTPS/域名 · cookie Secure · 首管强口令 · 备份 · 健康检查（详见 §6）。
22. ✅ **README 重写**(2026-07-02：更正 v0.1→当前能力/18集合/首用户引导/指向总纲) + ⏳ CHANGELOG/版本纪律。

### 阶段 4 · 抗趋同 + 打磨
23. 任务可靠性层 `aggregateByProfile` + 代际回归告警（护城河第3层）。
24. 可验签中立基准证书 + 黄金样例逐条打分（护城河第4层）。
25. 白标/i18n/无障碍/评论管理/分类页 等增强。

---

## 6. 部署上线要点（平台相关，阶段3 内落地）
仍必须：**schema 迁移体系**（生产关 push、启动前 migrate）· **密钥强随机**（你自己配一次）· **HTTPS+域名** · **cookie `Secure`**（`Users.auth.cookies`）· **首管强口令**（走 Payload 首用户引导，删文档明文弱口令）· **备份**（pgdata+media 一致性）· **健康检查**（`/health`+compose）· **限流/CSRF**（公网多用户升为必做）。
> 定位翻转后已**移除**：LICENSE 开源协议、密钥首启自举、多架构镜像、换域名不重建、面向陌生部署者文档。
> ✅ 已修：Media 本地卷 · DB idle 超时。

---

## 7. ⚠️ 待作者拍板（否则无法动阶段2 的"钱"）

**A. 真实数值（决定全盘，上线前用你实例小额真兑校准）**：New API 各模型**真实 margin 率** + **quota 刻度**（1 美元=多少 quota）；α(0.30)/β(0.15)/免费额度 F(30-50credit)/术值兑换率。
**B. New API 能力（贴一次真实 curl）**：是否 QuantumNous/new-api；建 token 能否设 group/remain_quota/expired_time(TTL)/日调用帽/model_limits；`/api/log` 能否读单用户消费明细+进货成本。
**C. 合规法务（3 件，必做）**：满额返利+虚拟奖是否规避有奖销售/赌博红线；credit/术值不可提现倒卖的协议条款；额度充值是否算单用途预付卡需备案。
**D. 中立性取舍（书面确认）**：接受榜单如实标"我更贵、BYOK更省"？加速/曝光绝不进随机池仅明码标价？是否愿花钱代跑竞品模型 benchmark？
**E. 反女巫**：同意补 ipHash/deviceHash？verified Runner 开通方式（人工/保证金/实名）？

---

## 8. 北极星指标 + 残余风险
**北极星**（这两个不起来一切空转）：① 每周有产出的作者数 ② 单位 Skill 累计真实样本数 × 当前窗口新鲜度。（非 Skill 总数/PV 虚荣指标）

**残余风险**（外生、产品无法对冲）：分发即正义（网关顺带看板截流）· solo 在真实用户量的资本消耗战处下风 · 隐私承诺(只存 sizeBucket)封顶护城河深度且与私人台账黏性冲突 · verified 手动置位是规模瓶颈 · 合规最终定性需属地律师 · 冷启动烧固定预算(P_boot+Free_boot≈¥500/月)。

---

## 9. 关键代码锚点（build 时直接定位）
| 事项 | 文件 |
|---|---|
| 聚合层重写(护城河第0层) | `src/lib/compat.ts`（aggregateByModel/recomputeLocalScore） |
| 水龙头/线上评测回流 | `src/app/v1/skills/[slug]/compare/route.ts`（skipAggregate） |
| register 术值漏洞 | `src/app/v1/auth/register/route.ts` + `src/lib/contribution.ts` + `src/collections/ContributionRules.ts` |
| 省钱路由 | `src/lib/route.ts`（selectModel）+ `routePolicy` + `MODEL_PRICES` |
| 私人台账 | `src/collections/SkillRuns.ts`（inputJson/outputText/model/estimatedCost） |
| New API 对接 | `src/lib/newapi.ts`（现仅 chatCompletion，需加管理 API 封装） |
| 中立评测榜(新建) | `src/lib/modelrank.ts` + `ModelPriceSnapshot`(新集合) |
| 签名/验签 | `src/lib/signing.ts` · `canonical.ts` · `runner/hengshu.mjs` |
| 术值/悬赏事务范式 | `src/lib/contribution.ts` · `src/collections/Bounties.ts`(frozenPoints/throwOnError/idempotencyKey) |
| 创作者发布(缺失,待建) | `src/app/(frontend)/` 下新增发布表单，参考 `src/components/BountyForm.tsx` |

---

## 10. 一句话收尾
**别人复制得了我的静态结论，复制不了我"持续为具体用户的具体 prompt 生成、越用越贵的活体运行资产 + 中立可验签基准 + 自有中转的省钱路由变现闭环"。眼下唯一今天就能干、且必须干的是阶段0 第1项——堵住正在印术值的 register 漏洞。**
