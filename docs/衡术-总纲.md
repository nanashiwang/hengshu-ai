# 衡术 Hengshu — 总纲（唯一开发依据）

> 状态：**开发蓝图 · 单一事实来源** · 更新：2026-07-03（资金红线复核后同步）
> 本文整合并取代此前 4 份规划稿（产品方向与护城河 / 成品化技术清单 / 生产部署清单 / 联动经济方案），后续开发优化**只依据本文**。
> 历史参考（不在本文范围、保留原文）：`hengshu_prd_v0.2.1.md`(原始 PRD) · `体验手册.md` · `PROGRESS.md`(功能进度基线，已冻结)。
> ⚠️ `hengshu_prd_v0.2.md` 与 `yuanheng_skillhub_product_doc_v0.1.md` 为**定位翻转前**旧稿（自部署时代），**勿作依据**，仅存档。

---

## 1. 产品定位

**衡术是一个中心化运营的 AI Skill 平台，"决策层 + 履约层"一体机**：
- **决策层**：中立评测 + 省钱路由——用真实运行数据告诉用户"这个 Skill 在哪个模型上真省真稳"，并自动帮你选当前实测最省够用的模型。
- **履约层**：作者自有的 **New API 中转**——模型调用真正发生的地方，平台从 margin 赚钱。
- **算力 = 用户自带 Key（BYOK），默认走自己中转**；平台不承担现金算力成本，从中转 margin 变现。
- **公网多用户中心化**（非自部署）；单一中心 → 兼容报告/术值榜/求术全站聚合，PT 网络效应成立，信任模型为中央签名。

---

## 2. 护城河与竞争力（战略核心）

**诚实前提**：单纯"攒兼容数据"不是护城河——结论低密度可爬、评分不奖励样本量、网关在数据上游、数据会腐坏、看一眼就走。**护城河必须挪到"长在真实 BYOK 流量上、且创造切换成本"的地方。**

**加固后的 5 层护城河**（均以已有代码为基座；标注"目标态"的部分尚未实现，勿当现状引用）：

| 层 | 是什么 | 为何抄不走 |
|---|---|---|
| **0 活体数据** | `compat.ts` 聚合层：现状=modelName 单级分组+时间衰减(halfLife=30天)+置信度折减；**目标态(阶段4)**=(modelName,modelVersion) 二级分组+漂移曲线 | 竞品抓走"今天"，抓不走连续过去与跨版本漂移曲线；腐坏反成壁垒 |
| **1 省钱路由** ⭐ | **#15已通电(2026-07-03)**：`recomputeRoutePolicy` 由真实回流回写 `routePolicy.dataDriven`，`selectModel` 优先个人历史→全站数据驱动，漂移随回流自刷新；控制台已显示"省钱回执" | 抄"看表"易，抄"真实回流驱动的自动降本路由"须先有等量真实运行 |
| **2 私人台账** ⭐ | 激活 `SkillRuns`（已存 input/output）做"我的运行台账 + 换模型一键重跑" | 用户**自己的**数据，非公共平均；迁移成本随使用线性上升；堵死 BYOK"查完去网关直连"漏点 |
| **3 任务可靠性** | 聚合键从 modelName 下移到 profile(skill×输入档×errorType)；代际回归告警 | 模型趋同后不消失反更值钱（prompt 工程质量差异不趋同） |
| **4 可验签基准** | verified + ed25519 升级为第三方达标证书；黄金样例逐条打分 | 网关自评自证有利益冲突，中立性结构上是你的 |

**⭐ 最锋利竞争力（复购与切换成本唯一来源）= 第1层×第2层**：
> **"绑你的 Key 后，用你自己历史 prompt 的真实回流，持续替你选最省的模型跑、漂移自动换路由、并累计告诉你这月省了多少钱。"**
> 个人化 + 每周涨可推送 + 离开就丢台账——省的钱沉淀在平台账本而非用户脑子里。

### 2.1 六维红队复核定论（2026-07-02：6 类候选 × 竞品红队，全部按最狠打法攻击）
**残酷事实：没有任何一层"今天"就成立**（评级：切换成本=C，数据/网络效应/信任/成本结构/标准=D）。但每层的成立开关都已定位，多为天级工作量。核心定论：

1. **数据不是独立护城河，是第1×2层的派生物**。壁垒深度公式 = **独占流入速率 × 半衰期**，不是累计条数。规模阈值有算术：全站需 ~77 次真实运行/天才能让 25 Skill×4 模型的兼容矩阵整体"满置信活着"——低于此值护城河深度记零，不对外宣传。"网关没有 Skill 语义"防线偏弱（response_format/json_schema 可让网关推出等价 formatValid），真独占的只有：**私人台账（ownerOrAdmin 天然不可爬）+ 真实用户自然流量分布 + Skill 语义连接键**。
2. **两面承重墙（红队打不穿的仅有位置）**：① **个体切换成本**——SkillRuns 私人台账 × 省钱回执（累计省钱额是唯一**不随 30 天半衰期归零**的单调增长资产，留存钩子全押它）；不需要网络密度，solo+¥500/月唯一养得起的形态。② **跨中转中立元台账**——国内 New API 中转极度碎片化，没有任何单一网关看得到用户跨中转的全量调用；衡术定位为"跨中转的中立元台账/元路由"是**唯一对"网关上游超集"攻击免疫**的结构位置。
3. **counter-positioning 只对"自有模型利益"平台成立**（Coze/豆包、百度系抄真诚中立=自噬），**对中立聚合网关不成立**（OpenRouter 本就中立+有 :floor 价格路由，AWS Cost Explorer 证明"教客户省钱换留存"不是禁区）。低固定成本=氧气不是墙：它买的是熬到第1×2层长成的 12-24 个月时间。
4. **信任层单独不成立，只能当第0-2层的"信用放大器"**，且必须机器强制而非口号：2026-07-02 红队时四面墙为 0/4；截至 2026-07-03 已落地纯排名断言、签名快照、公开验签与外锚导出，仍需外部时间戳、官方价真值和真钱校准继续加固。先自曝 margin 政策（披露在先=透明，被揭在后=丑闻）；SkillRuns 明文 input/output 曾是第2层的**信任毁灭开关**，现已加密；访问审计已补，备份加密/离机/恢复演练已纳入 preflight 门禁，生产仍需实际填入确认。
5. **冻结两个叙事**：❄️"推 Skill Spec 事实标准"（solo 无分发杠杆+大厂格式收敛窗口期，最差时点；改做 import 适配器搭 Claude Skills/GPTs 便车，fork 谱系按"变异-表现差分数据采集器"立项）；❄️"网络效应"（唯一半通电的数据回路电流≈运营者本人；冷启动期不当卖点）。PT 邀请制**分层收缩**：只锁"挣术值/verified 计分/兑换"经济写路径，读+限额试跑放开流量（online 权重 0.3+封顶 3 条+6a 扣费已兜底）。

### 2.2 第二轮非常规候选红队定论（2026-07-02：合规/中文垂直/中转联盟/PT身份/失败知识库/B2B 采购）
**三个 B 级幸存者（全部是焊在承重墙上的钢筋，非第三面墙）：**
1. **中文×国产模型评测垂直（B）**——英文界(LMArena/AA)结构性不做、国产厂商自评=自噬、学术榜(SuperCLUE)不做任务级中文风格实测（公文体/小红书体/成语/¥千字真实分词成本）。是墙①②共同短板"获客"的引擎+信用钢筋。抄不走的只有：跨版本漂移时序、私有 held-out 套件+rubric、失败案例回路——价值曲线前 6 个月≈0，窗口期 6-18 个月（中立中转站入场前）。建设与既定必建项高度重叠（ModelPriceSnapshot=§3.3墙3、签名快照=6j、周榜页=#12 modelrank），增量仅 3-5 天 rubric 人肉+每月 ¥60-160 跑测费。**验收线：8-12 周"周报→注册→绑Key"转化不达标即降级月更**。
2. **失败知识库/负知识（B）**——没人系统攒"什么在哪个模型上怎么坏+怎么修"；单条确认案例即"救命"价值（不需统计显著性，匹配低流量现实）；SEO 长尾（"XX模型 json 截断"）竞争密度≈0，是 ¥500/月下唯一高意图获客渠道。喂路由后升维"成本×可靠性最优"（OpenRouter :floor 纯价格路由做不到）。✅ 水龙头已打开且 `/failures` 已上线：公开页只给症状+根因摘要，修复模板+复验步骤锁登录后。
3. **PT 社区身份（B）**——机理修正：PT 活二十年靠的是"身份系统包裹稀缺品"，稀缺品在衡术=两面墙，身份只是配给层+放大层。**身份必须焊在省钱回执上而非归属感上**（这个人群"忠于省钱不忠于站"）；名号绑可验证真实数据+ed25519 快照。只做"产品功能"（个人主页/名号/创始编号/邀请谱系，5-7 天一次性），**绝不做论坛/群聊/广场**（UGC 先审后发义务+solo 时间黑洞双禁区）。创始编号 #001-100 每晚一天少一天，0.5 天先落。
**一张 C 级期权：中转联盟 probe-first**——把"中转抄你"反转为分发的唯一路径；但徽章无信誉时 BD 必碰壁 → 先用黄金样例探针跑"中转体检榜"（签名+外锚），静默 3 个月看 inbound：≥3 家主动求徽章则推进，零则永久降级为墙②自产数据源。防 Dieselgate 探针欺骗从第一天设计（随机化改写/普通用户账号/金丝雀题）。
**两个明确不做：** ❌ 合规当护城河（对有钱对手零阻挡；正确记账=经营许可证而非竞争壁垒，触发器=recharge 上线时点）；❌ B2B 合规采购（有合规痛点的企业买云厂商 MaaS，用中转的企业恰是绕开采购的人；且合规聚光灯会烧毁墙②生态位——唯一可能负外部性的候选。两年后以"团队版台账+国产 BYOK 通道"薄片重启，触发器=月自然企业询单≥3）。
**🚨 红队新发现的自爆装置（升级为 6l，与 6a 同改动点）**：当前 `runSkill` 默认 fallback=claude-haiku（境外未备案）+ 无 BYOK 静默回退平台 Key 代理——与 2026 年被整治的中转站同一法律位置（服务提供者定性）。72 小时架构切割：默认/fallback 链换已备案国产模型，境外模型仅 BYOK 显式配置可用。

---

## 3. 经济模型（变现，且反哺护城河）

### 3.1 三币单向漏斗
```
人民币 ──充值──▶ credit(算力燃料) ──跑模型──▶ 蒸发
贡献行为 ──挣──▶ 术值 ──受控兑换──▶ credit
                     术值/credit 永不反向变现金 ❌（保命红线：防资金盘/赌博）
```

| 币 | 角色 | 护栏 |
|---|---|---|
| CNY | 唯一入金口（New API 兑换码充值） | 衡术不碰现金；无任何→CNY 出口，代码硬禁止 |
| credit | 唯一花算力的币(1credit=¥0.01零售) | 物理映射为受限子令牌（低价分组+配额熔断+TTL）；free 档不产术值/不可兑换转让→白嫖上限=进货成本，套现≈0 |
| 术值 | 贡献信誉分（不可买只能挣） | 高价值行为**被采纳才发分**；不可转赠；永不进随机池 |

**核心不变量（结构性防亏）**：术值兑换池 = **α × 当月已实现毛利**(α=0.30) → 先赚到才有得兑，**永不亏 margin**，最坏少赚 30%。

### 3.2 抽奖的最终结论：**不做纯抽奖，做"满额返利宝箱"**
纯抽奖/开箱踩两条红线（反不正当竞争法有奖销售 + 变相赌博）。改：**充值满额 100% 必得**确定返利(6% 公示)，**随机只决定返利形态**（都是额度券，用户永不空手）——类"第二杯半价+随机送小料"。术值绝不进随机池。⚠️ 上线前走一次律师。

### 3.3 中立与变现共存：四面墙（护城河不被变现稀释）
1. **数学隔离**：`modelrank.ts` 排名函数入参**无 margin/isOurs 字段** + CI `grep` 断言（人事后也改不动）。
2. **记账隔离**：benchmark 按成本价记账(margin=0)；履约 SkillRuns 不进榜分。
3. **成本真值隔离**：用公开官方报价快照(`ModelPriceSnapshot`)非平台可编辑价；双列"官方价 vs 我到手价"，更贵必红标"BYOK更省"。
4. **履约隔离**：run 响应强制回传 `cheaperViaByok`，更省走 BYOK 不得隐藏。

### 3.4 New API 对接
1 个 New API 账号 = 全体用户，靠**子令牌**隔离（`token_name="hs_<userId>"`）。**已定架构（优于原稿）**：权威 credit 账本在本平台 `credit-logs`（原子事务+幂等），网关配额同步是提交后的**副作用**（失败不影响账本，靠对账 worker 补偿）——而非原稿"先扣术值→PUT /api/token→失败回滚"的跨系统事务。注册发受限子Key(60秒真跑)；cron 拉 `/api/log` 用量真值回流喂护城河(剥离明文)并回填当月毛利。

---

## 4. 现状与差距

### ✅ 已建成（S1-S7 + 信任模型，代码核实真跑通）
下载→安装→离线运行(Runner CLI)→兼容回流+LocalScore→术值/贡献2.0→求术悬赏→manifest ed25519 签名；前台首页/市场/详情/排行/悬赏/控制台；评论收藏闭环。
**本会话已修**：求术钱路原子化+幂等 · Media 本地卷 · DB idle 超时。

### ❌ 关键差距（2026-07-02 五维复盘后的真实现状；历史红项已修见 §5 各✅）
> 已修不再列：创作者供给断路✅(#7) · register 印术值✅(#1) · 数据一致性批次A+对账✅(#5) · 测试CI基线✅(#20) · 前端基建✅(#19)。

| 缺口 | 严重度 |
|---|---|
| **credit 变现闭环仍待真值校准**：✅充值码入口+runSkill 消费出口已补；✅运行台账已明确区分 `chargedAmount`(元) 与 `chargedCredits`(credit)；✅credit/术值账本已加用户级咨询锁防并发丢更新；✅兑换池已强制要求本月 worker 对账毛利(`marginSource=newapi/local`+`marginReconciledAt`)才会生效；✅New API 对账已升级为 `/api/log` 逐条提取输入/输出/cache 创建/cache 命中 token，优先结合日志 `other` 的实际 `model_ratio/completion_ratio/cache_ratio/group_ratio` 自动精算，缺失时回退 `/api/pricing` + `/api/status` 的价格快照；`NEWAPI_MODEL_MARGIN_RATES` 仅保留 dry-run 对照；但 `/api/log/self` 个人日志口径、真实价格快照与真钱小额闭环仍未验收 | 🔴🔴 资金红线（真钱闭环未校准前**不要上线兑换**） |
| **烧钱端点护栏已补，仍待真实网关隔离验证**：credit 预检/实扣、compare 串行、平台代付国产白名单、真实模式用户子令牌已补；✅ 代码级测试已锁死 BYOK 显式 Key 失败不回退平台全局 Key；仍需用真实 New API 小额验证部署侧不会打到平台账 | 🔴 上线前必验 |
| ✅ SkillVersions.update 越权已修；✅ 草稿版本 REST 读取已按发布状态/作者/审核员收口 | 🟢 |
| ✅ 删除用户 credit 阻断/级联与 creditBalance/favoriteCount/avgRating 对账已补；✅ 生产重复数据预检 worker 已补；仍需真实库去重与迁移实跑 | 🟡 |
| ✅ 收藏循环刷分已用 actor+idempotencyKey 根治；一次性奖励并发仍靠唯一索引兜底 | 🟢 |
| ✅ 封禁机制已补，active-session 端点、Runner Bearer、Payload 集合 access 与后台 admin 入口均已拦截；✅ `docs/安全运营SOP.md` 与 `audit-logs` 已补 | 🟢 |
| ✅ Runner token 哈希+TTL、轮换/撤销 UX、生产默认禁用旧明文 token 鉴权、设备授权临时 token 加密、BYOK 加密、register 防枚举、设备码来源确认+申请频控已补 | 🟢 |
| ✅ 通知系统 / 用户设置 / 举报处置端已补；BYOK 已加密存储 | 🟢 |
| ✅ 高频查询索引已补；✅ migration 基线已生成；✅ 生产重复数据预检 worker 已补；生产上线仍需真实库迁移与数据清理 | 🟡 |

---

## 5. 统一落地路线（唯一开发顺序）

> 合并了原部署/成品/护城河/经济四份的所有优先级。按阶段推进，每阶段独立冒烟。

### 🚨 阶段 0 · 立即止血 + 护城河地基（本周，多为 0.5-2 天/项，不依赖外部决策）
1. **修 register 术值漏洞** ✅限速已做(2026-07-02，对抗审查确认正确无回归)：register 改为依赖规则表(不传 points)；invite/skill_version_update 加 `dailyLimit=3`。✅ 2026-07-03 续补：注册 IP/device 双哈希已接入，同 IP/同设备自邀不发术值；同邀请码注册已加事务+咨询锁，避免并发复用双建用户。残余为清浏览器、换设备/跨网仍可绕，只能靠邀请制+账本风控继续压。
1b. **修 skill_published(+50) 反复发布刷分** ✅已修(2026-07-02，对抗审查通过)：afterChange 发放 `idempotencyKey=pub:<skillId>`，由 contribution-logs 唯一索引硬幂等；反复 published↔非published 与并发双发都不会重复奖励。注：全局 `(relatedSkill,actionType)` 唯一约束会误伤 skill_run/favorited 等每 Skill 多条日志的行为，**不可行**。
2. **`compat.ts` 聚合层重写** ✅已做(2026-07-02，对抗审查通过)：时间指数衰减加权(halfLife=30天，近期主导抗腐坏) + LocalScore 置信度衰减(有效样本<10 则按比例压分，杜绝 N=1 的 100% 噪声霸榜) + decayWeight 缺时间戳按 0 权重(防"无时间=最新"刷分)。✅ 2026-07-03 续补：聚合读取改为近 180 天窗口 + 500 条分页，并给 `compat-reports(skill,createdAt)/(modelName,createdAt)` 补索引，避免 `limit:5000` 全量同步重算与旧数据腐坏。**残余**：① confidence 对长期无回访的旧优质 Skill 会指数压到 0(与"无数据"不可区分)，考虑加地板或区分展示 ② (modelName,modelVersion) 二级分组/漂移曲线留阶段4。
3. **水龙头·在线评测回流** ✅已做(2026-07-02，对抗审查通过)：在 `runSkill` 里为真实非 mock 调用回流 `source='online'` 兼容报告（不含原文，仅分档/成败/耗时），在线试用与多模型对比都喂护城河。**抗刷三重**：排除作者自跑 + 单用户对同一(skill,model)封顶3条 + 来源分级权重(verified=1/community=0.5/**online=0.3**)削弱刷分投毒杠杆。**残余**：作者用清浏览器/换设备/跨网小号仍可绕自跑排除；localScore 缓存不在热路径刷新(靠 report/worker 收敛，反而更安全)。
4. **最小样本门槛** ✅已做(2026-07-02)：单模型报告<5 则前端显示"战绩积累中·已N次"(colSpan 跨三列)而非误导性百分比(`aggregateByModel` 返回 lowSample，详情页据此渲染)。
5. **数据一致性地基**：✅**批次A已做**(2026-07-02，对抗审查通过)——`Favorites(user,skill)`/`Reviews(user,skill,type)`/`SkillInstalls(user,skill,runner)` 复合唯一约束 + favorite/install 竞态兜底(回查确认再兜底) + register 同邀请码事务锁 + `Categories`/`RunnerClients` beforeDelete 清孤儿(含 device-codes) + ReviewForm 唯一冲突友好提示。⚠️ **上线前必须先去重**：favorites/reviews/skill-installs 若有历史重复行，dev push 建唯一索引会失败（无 migrations），先跑去重 SQL。**残余**：① ✅ **Users 删除保护/级联已做**(2026-07-02，对抗审查通过：阻断有作品/进行中悬赏者 + 阻断有资金/术值流水用户删除 + 删私有从属触发计数修正 + 解引用保留历史；补全 reports/skill-versions 遗漏外键；原子回滚。Payload postgres `_rels` cascade 特性已确认——遗漏字段是静默丢引用而非删不掉) ② ✅ 2026-07-03 续补：`SkillInstalls` 已安装状态必须绑定 Runner，撤销 Runner 改为删除该 Runner 的安装记录，生产预检会阻断历史 `installed + runner=NULL` 脏数据，避免绕过复合唯一约束 ③ ✅ **指标可从 SkillRuns 重算已做**(2026-07-02，对抗审查通过)：`worker:reconcile` 台账对账 worker——术值重算为 SUM(logs.points)、Skill 指标由 skill-runs 唯一重算，默认 dry-run/`--apply` 写回；配套 SkillRuns 加 `countedInMetrics` 持久化"是否计入指标"意图(compare 探测=false)防错计；剥离 rank(依赖 wall-clock 非可重算) ④ ✅ **余额并发已大幅缓解**(2026-07-02)：awardContribution 结算金额守卫(bounty/consume/other 绕过规则表防误伤) + update+create 原子化(自开事务保 `contributionScore==SUM(logs)` 不变量)；真正 SELECT FOR UPDATE 行锁仍未做，但"原子写入 + 对账 worker 周期性纠偏"已覆盖 read-modify-write 丢更新的实际风险。
6. **反女巫地基** ✅ipHash 已做(2026-07-02，对抗审查通过) + ✅deviceHash 已做(2026-07-03)：Users 加 IP/设备 HMAC 哈希(admin-only/hidden)；register 采集 IP(**从右往左取 XFF 防伪造**+`TRUSTED_PROXY_COUNT`) 与前端 localStorage 随机设备 ID → 同 IP 24h 注册宽松频控(20) + 同设备 24h 频控(5) + 同 IP/同设备自邀不发术值。**残余**：① XFF 需生产反代 append 真实对端才可信(部署时配) ② localStorage 设备 ID 可被清浏览器/换设备绕过 ③ 注册快照不能防后续跨网迁移 ④ PAYLOAD_SECRET 生产缺失已 fail-fast，dev 仍用固定盐兜底 ⑤ 同网/同设备真人互邀不计术值(需 FAQ 明示)。

### 🚨 阶段 0.5 · 复盘补洞（2026-07-02 五维审查+对抗核验产出；先于阶段1 剩余项）
> 进度：✅ 6a·6b·6c·6d·6e·6f·6g·6h·6i·6k·6l·6m + 6j-1/2 + 公开验签/外锚导出已做｜⏳ 生产迁移实跑
6a+6l. ✅ **credit 消费出口 + 烧钱护栏 + 合规架构切割已做**(2026-07-02，金融+安全对抗审查通过)：默认/fallback 模型换 deepseek-chat；平台代付(未绑Key走平台Key)仅限已备案国产白名单(`approvedPlatformModels`)，境外模型仅 BYOK；runSkill 三重护栏(每用户60秒频控+白名单+credit预检)；真实调用后 `applyCredit(consume,幂等键 run:runId)`，BYOK不扣/mock全免；402/403/429 映射；注册赠送额度 `freeCreditOnRegister`。**P0修复**：compare 改串行消除并发预检TOCTOU白嫖4× · maxTokens硬约束防预检失效 · seed routePolicy 已改为国产白名单模型；历史全境外配置在平台代付仍会降级国产兜底(forceModel仍提示需BYOK)。**新增进展(2026-07-03)**：平台代付真实模式改走用户 New API 子令牌，避免全局 Key 绕过配额；生产环境若 New API 管理 API 未配置则平台代付 fail-closed，生产缺模型网关也禁止 mock 输出；注册赠送/兑换/充值/运行扣费后按本平台 `creditBalance` 同步子令牌**绝对 quota**，避免并发 delta 覆盖与网关 quota 漂移累积；SkillRuns 增 `chargedCredits`，`chargedAmount` 明确为元；新增 `worker:backfill-register-credit` 补偿历史注册赠送漏发并同步 quota；RunStudio 已标注"平台代付/BYOK/需BYOK"，未绑 BYOK 时禁选非白名单模型。**新增进展(2026-07-03 续补)**：Redis 原子 `INCR/EXPIRE` 分布式限流已接入：平台代付路径 Redis 缺失/不可用 fail-closed，BYOK 路径可降级 DB 窗口计数；充值码资金入口也按用户做 10 分钟严格 Redis 限流，生产缺 Redis fail-closed；✅ `newapi` 单测锁死 BYOK 显式传入时只用用户 Key，401 也不回退平台全局 Key。**残余**：坏BYOK Key网关计费隔离仍需生产部署小额实测。
6b. ✅ **SkillVersions 越权修复已做**(2026-07-02，真机验证嵌套查询支持)：update 改 `ownSkillVersionOrStaff`(按 skill.author 过滤) + beforeValidate 钩子纵深防御(覆盖 create 指向他人 Skill)。✅ 2026-07-03 续补：SkillVersions.read 按已发布公开/作者/审核员/管理员收口，草稿 Prompt 不再全网可读。
6c. ✅ **删用户资金保护 + 对账扩展已做**(2026-07-02，4段对账实跑通；2026-07-03 续补 append-only 台账保护)：Users.beforeDelete creditBalance≠0 阻断(防真金蒸发)，且存在 credit/contribution 流水时禁止删号(改走封禁/匿名化)，避免删除 append-only 资金/术值历史；reconcile worker 新增 ③credit(creditBalance==SUM) ④计数(favoriteCount/reviewCount/avgRating 由 visible reviews 重算)。
6d. ✅ **skill_favorited 循环刷分幂等已做**(2026-07-02)：ContributionLogs 加 actor+idempotencyKey(唯一索引)基建；awardContribution 支持幂等键；Favorites 用 `fav:<actor>:<skill>` 一次性发放；skill_published 改 `pub:<skillId>` 根治并发双发。
6e. ✅ **封禁机制已做**(2026-07-02)：Users 加 accountStatus(active/banned) + beforeLogin 拦截 + run/exchange/发布Skill/发布悬赏/接单/提交 active-session 校验 + ✅ **Reports 一键处置端封禁闭环**(#17) + ✅ **封禁用户历史 online/Runner 报告追溯降权**(user anonHash + runner relation + runnerId anonHash 均置 suppressed=权重0) + ✅ 封禁后尽力把 New API 子令牌 quota 归零 + ✅ `docs/安全运营SOP.md`。
6f. ✅ **安全杂项已做**(2026-07-02)：register 防枚举 · runSkill 失败不透传上游原文 · bounty submit 校验交付归属。✅ **2026-07-03 补强**：BYOK AES-GCM 加密、SkillRuns input/output 加密、Runner token 哈希+TTL、生产默认禁用旧明文 token 鉴权(`/ALLOW_LEGACY_RUNNER_TOKEN_AUTH=1` 仅迁移窗口)、设备授权轮询临时 token 加密存放且消费即清、/v1 写请求同源校验。✅ 2026-07-03 续补：device authorize 二段确认 + 设备码申请频控 + 控制台 Runner 撤销(`POST /v1/runners/{id}/revoke`，删除实例并解除安装/报告/设备码引用) + Runner 自助轮换(`POST /v1/runner/rotate`/`hengshu rotate-token`，新令牌只返回一次、旧令牌立即失效)。新增 `worker:migrate-secrets` 可加密历史 BYOK 并清除历史 Runner 明文 token（本地已 apply，dry-run 复核待处理=0）。✅ 2026-07-03 再补：Payload 集合 access 对 banned active session fail-closed，防绕过 `/v1` 端点直打 REST 创建/修改资源。✅ 2026-07-03 再补：模型网关错误体统一脱敏，失败 SkillRun 只存受控错误文案，避免上游回显 Key/内部错误进入台账或日志。✅ 2026-07-03 再补：`audit-logs` 安全审计台账，覆盖 BYOK 设置/清除、Runner 撤销/轮换、举报处置、充值码兑换、术值兑换；metadata 统一脱敏。✅ 2026-07-03 再补：BYOK 密文字段禁止 Payload 后台/REST 直读直写，服务端 override 写入也统一归一化加密，防绕过 settings route 落明文。✅ 生产备份加密/离机/恢复演练已纳入 `worker:preflight-production` 门禁。残余：上线时真实填入备份确认并保留恢复演练证据。
6g. ✅ **热路径索引已做**(2026-07-02)。
6h. ✅ **迁移基线已生成**(2026-07-03)：`src/migrations/20260702_173109_p0_hardening_and_recharge.ts` 覆盖 P0 加固 schema，`20260702_180021_margin_reconcile_gate.ts` 覆盖兑换毛利对账门禁，`20260702_181548_recharge_code_hash.ts` 覆盖充值码 HMAC 化，`20260702_184401_skill_run_charged_credits.ts` 覆盖运行台账扣费单位拆分，`20260703_052000_notification_and_compat_indexes.ts` 覆盖 `skill_updated` 通知枚举与 compat 复合索引；`20260703_060000_user_device_hash.ts` 覆盖设备哈希；`20260703_063000_skill_submission_key.ts` 覆盖发布 Skill 幂等键；`20260703_064500_audit_logs.ts` 覆盖安全审计台账；后续 schema 变更继续走 migration；`worker:preflight-production` 已扩展为**生产强配置门禁 + New API 在线权限门禁 + 备份加密/离机/恢复演练门禁 + local 毛利估算显式确认 + favorites/reviews/skill-installs 重复数据预检 + installed 无 Runner 脏数据预检**，会阻断弱 `PAYLOAD_SECRET`、HTTP/缺失/不同源的 `SERVER_URL`/`NEXT_PUBLIC_SERVER_URL`、缺 Redis/网关/New API 管理令牌、`/api/token` 不可用、`NEWAPI_USAGE_SOURCE=newapi` 但 `/api/log` 不可用或 token_name 过滤不可证明有效、签名私钥缺失、sk 模型 Key 冒充管理令牌、平台代付白名单混入境外模型/显式空白名单、非法子令牌 TTL、开启旧 Runner token 鉴权、未确认生产备份等上线风险，并在 NEWAPI_SUB_GROUP 缺失且未显式确认默认分组安全时阻断，避免平台代付落到高价/非受限默认分组。残余：若接入已有历史库，上线前仍需按预检结果去重并实跑迁移。
6i. ✅ **封 CompatReports 时序外泄已做**(2026-07-02)。
6j. 🟢 **四面墙机器强制**：✅ **6j-1 数学隔离断言已做**(2026-07-02) + ✅ **6j-2 分数 ed25519 签名 append-only 快照已做**(2026-07-02，随 #12，真机验证签名落库)。✅ 2026-07-03 续补：`/verify` 公开验签页 + `worker:export-score-anchors` 导出 `docs/anchors/score-snapshots.jsonl` 与 manifest，并为每行生成 entryHash/chainHash 哈希链；`worker:verify-score-anchors` 可在发布外锚前校验链与 manifest；提交公开 git 后形成外锚。
6k. ✅ **关 Runner 未签名静默放行洞已做**(2026-07-02)：installSlug 默认强制有校验和+已签名+签名有效，`--allow-unsigned` 显式豁免；生产须配 HENGSHU_SIGNING_KEY。
6m. ✅ **打开失败回流水龙头 + 错误分类已做**(2026-07-02，对抗审查通过)：`errorTaxonomy.ts` 两层受控分类(infra/capability)，仅存标签不存原文；skillRunner 失败运行也回流 compat-report(带 errorType)——负知识库唯一原料来源，此前完全丢弃；SkillRun.errorCode 结构化；CompatReports.errorType 加索引。✅ 2026-07-03 续补：`/failures` 失败知识库已产品化，公开只展示症状/根因摘要，登录后展示修复模板和复验步骤。
### 阶段 1 · 供给命脉 + 护城河数据源（成败关键）
> **复盘定论（依赖倒置纠偏）**：#13 兑换已建成但其价值依赖"术值有意义"→依赖"有作者产出+真实运行"。剩余项按北极星杠杆重排：**#8 发布即评测 → #9/#10 冷启动 → #15(前半:routePolicy 数据回写) → #16 台账+#17 通知(合并一迭代,共享控制台读视图) → #12 四面墙 → #14/#18 推迟**。
7. **前台"发布 Skill"表单** ✅已做(2026-07-02，对抗审查通过：安全/越权/原子/回归全过)：`POST /v1/skills`(登录即可，status 硬编码 pending、author 硬编码本人、白名单字段防注入)事务内建 Skill+首版本、版本 afterChange 自动设 currentVersion；前台 `SkillForm`+`/console/skills/new`+侧栏入口；单作者 pending 存量上限 20 防刷。✅ 2026-07-03 续补：客户端 `idempotencyKey` + `Skills.clientSubmissionKey` 唯一索引，超时重试/并发重放返回同一 Skill；Skills 自定义 update access 也纳入 banned fail-closed。**残余**：① ✅ "我的作品"页(A4，`/console/skills`)+ 草稿预览(A5，作者/审核员可在详情页预览自己未发布 Skill) 已做(2026-07-02，安全审查确认不泄露)。
8. **发布即评测**：✅ **机制+worker 已做**(2026-07-02，真机跑通)：`runSkill` benchmark 模式(系统评测：免频控/预检/扣费、不排除自跑、source=benchmark 权重1、受国产白名单约束、skipAggregate 不污染履约指标) + `src/lib/benchmark.ts`(遍历 examples×模型→重算 LocalScore) + `worker:benchmark <slug>|--all`(可控成本播种，对齐 #10)。✅ **2026-07-03 续补**：发布通过审核后自动入 Redis benchmark 队列，`worker:benchmark-queue` 串行处理；`BENCHMARK_QUEUE_MAX_JOBS`/`BENCHMARK_MAX_ATTEMPTS_PER_SKILL`/`BENCHMARK_MODELS` 做成本上限与模型控制；Redis SET NX 24h 去重避免重复烧钱。✅ 官方种子已改用国产白名单模型，避免平台 benchmark 被境外模型卡住。仅 worker 可触发→无刷分面。**残余**：① 单样例仍 lowSample(靠 online 回流累积置信) ② 上线需确认网关分组实际可调用 `APPROVED_PLATFORM_MODELS` 里的国产模型(否则 503)。
9. **5 种子标"可 fork 模板"** + 首页"从改一个现成的开始"：✅ **fork 机制已做**(2026-07-02)：Skills.forkedFrom 血统 + `POST /v1/skills/[slug]/fork`(复制源当前版本全字段到自己名下 pending，剥离源 dataDriven) + 详情页"🍴从这个改一个"按钮 + 血统徽章。✅ 首页模板区已做(按已发布 public Skill 的 SkillRank 取前 3 个作为 fork 起点)。残余：同血统差分分析(阶段4)。
10. **作者亲手灌 20-30 个满数据 Skill**：✅ 代码种子已扩到 20 个官方 Skill，覆盖内容/办公/客服/评测/教育/代码，并全部使用国产白名单云模型；残余是生产跑 `npm run seed` + `worker:benchmark-queue` 或 `worker:benchmark -- --all` 产生真实样本（会花 API 成本）。
11. **New API 子令牌联动**：✅ **RealAdmin 已实装**(2026-07-02，按 QuantumNous/new-api 源码规格)：`newapiAdmin.ts` 三方法真实调用(建/改令牌 `POST/PUT /api/token`、日志 `GET /api/log`)，鉴权 `Authorization+New-Api-User` 双头，credit↔quota 换算(QuotaPerUnit=500000)，全 env 配置、未配=stub 兜底，mock 单测锁死请求形态；注册预建子令牌 + 兑换/充值后同步配额。✅ 2026-07-03 续补：新增 `setQuotaToCredits` + `syncNewApiQuotaToBalance`，以本平台余额设置网关绝对 quota，且真实模式运行时强制显式配置 `NEWAPI_CREDIT_TO_QUOTA` 并校验为正数，所有配额 PUT 都强制 `unlimited_quota=false`，并把子令牌 `model_limits` 收口到平台国产白名单，显式空白名单会在生产 preflight 与 New API 下发前 fail-closed，既有子令牌若模型限制、低价分组或 TTL 缺失/漂移会补写白名单/分组/滚动过期时间；缺 `NEWAPI_SUB_GROUP` 时默认 fail-closed，除非显式 `ALLOW_DEFAULT_NEWAPI_SUB_GROUP=1` 确认默认分组安全；`NEWAPI_SUB_TOKEN_TTL_DAYS` 必须在 1-365 天内；发现同名 `hs_<userId>` 重复子令牌会先全部清零再 fail-closed；token 列表或 `/api/log` 触达分页扫描上限、任一消费日志缺有效 quota 字段、缺失/非消费 type=2、退款/异常流式结算、缺失/不匹配 token_name、缺失时间戳、早于 start_timestamp 或荒谬未来时间时也 fail-closed，避免偷用开发默认刻度、非法刻度、永不过期/异常长效子令牌、同名旧令牌残留、历史无限配额、高价默认分组、深分页漏扫、用量截断、过滤失效混入他人日志、时间窗过滤失效、未来日志污染账期或混合非消费/退款/异常流式/无 quota 日志低估放大真钱/合规风险；平台代付真调用前先按本平台当前余额覆盖子令牌 quota，运行扣费成功后再回推绝对 quota；`fetchUsage` 已分页拉全 `/api/log` 并显式返回原始 usedQuota，并逐条复核日志 type=2、归属 token_name 与时间窗，月度对账会同时比较全站与逐用户的 New API quota 折算消费、本地 credit consume 漂移，避免月日志超过 1000 条被截断、type/token/time 过滤参数失效、未来时间记录、退款/异常流式结算、无 quota 字段或扣费口径漂移时误验收；新增 `worker:sync-newapi-quota` 批量补偿同步(封禁用户 quota=0)。✅ 2026-07-03 续补：新增 `worker:calibrate-newapi -- --apply`，只能使用 `calib-` 前缀临时子令牌，先验证 `/api/log` 权限，再下发小额 quota(0.01-10 credit 显式边界，非法值不静默 clamp)、发起真实低 token 调用、检查日志新增消费记录、quota 消费增量且不得超过下发临时 quota，并验证坏 BYOK Key 被拒且不新增 quota 消费；临时子令牌一旦确认存在，校准结束/失败后都尽力把 quota 清零，清零失败则整次校准失败并提示人工处理。✅ 2026-07-03 再补：`worker:preflight-production` 会在线限时探测 `/api/token`、`/api/log`、`token_name` 过滤、`start_timestamp` 未来窗过滤与日志样本退款/异常流式结算，单项网络失败会记录为阻断项而不是吞掉其他结果，`NEWAPI_USAGE_SOURCE=newapi` 且 `/api/log` 不可用、token/time 过滤不可证明有效、日志样本结算口径异常，或毛利率/漂移容忍/用量来源配置非法时在连接数据库前阻断上线；`worker:reconcile-newapi -- --apply` 会输出逐用户漂移 action 并导出 JSONL 处理清单，发现用户级多扣/少扣即阻断毛利写回；JSONL 导出路径只能是 `.jsonl` 普通文件，拒绝软链接，临时文件原子替换，写出后强制 `0600`，避免误覆盖配置或泄露用户金额。✅ 2026-07-03 再补：`/api/log` 记录会提取 `model_name/modelName/model` 与 input/output/cache read/cache creation token；`worker:reconcile-newapi -- --apply` 在 `NEWAPI_USAGE_SOURCE=newapi` 下优先读取 `/api/pricing` + `/api/status`，优先按日志 `other` 中实际结算倍率复算 token 成本，缺失时再按 New API 模型倍率、completion/cache 倍率、`NEWAPI_SUB_GROUP` 分组倍率、`quota_per_unit`、`usd_exchange_rate` 回退复算，并与 New API quota 实扣漂移比对；`NEWAPI_MODEL_MARGIN_RATES`/`NEWAPI_MARGIN_RATE` 仅作 dry-run/旧口径对照，不再作为真钱写回主路径；`worker:preflight-production` 同步在线探测 `/api/pricing`、`/api/status`。**实测(2026-07-03)**：当前 cn.meta-api.vip 管理 access token 可访问 `/api/token`，但 `/api/log` 返回权限不足；同一令牌可访问 `/api/log/self` 与 `/api/log/self/stat`，已支持 `NEWAPI_LOG_SCOPE=auto` 在 admin 日志不足时退到 self 日志，再用 `token_name=hs_<userId>` 验证子令牌隔离；模型 `sk-...` Key 不是管理 access token；`worker:newapi-probe` 已输出无泄漏分类提示，并在 log/self token-time 过滤不可证明有效、pricing/status 不完整时 fail-closed。**残余设计(需真实设置后定)**：spend 授权模型一致性(网关扣 remain_quota vs 本地 credit-logs) · /api/log/self 需用真实子令牌小额调用确认 quota 字段语义、模型/token 字段、pricing/status 快照与 quota 实扣偏差。
12. **中立四面墙**：🟡 **大部已做**(2026-07-02)：✅ modelrank.ts 纯排名(受 6j-1 CI 断言实弹守护) + ✅ ModelPriceSnapshots 官方价快照(成本真值隔离) + ✅ `/models` 中立看板(四维排序+方法论公示) + ✅ benchmark margin=0(source=benchmark 不扣费) + ✅ run 回传 cheaperViaByok(履约隔离) + ✅ **6j-2 分数 ed25519 签名 append-only 快照**(改历史必留痕，真机验证签名落库) + ✅ `/verify` 公开验签页 + ✅ `worker:export-score-anchors` 外锚导出脚本（JSONL + manifest + entryHash/chainHash 哈希链）+ ✅ `worker:verify-score-anchors` 发布前验链 + ✅ `/models` 双列"官方价 vs 我到手价"展示，平台价高于官方价红标 BYOK 更省，且排名不使用平台价。**残余**：① 将导出的 `docs/anchors/score-snapshots.jsonl`/manifest 提交到公开 git/OpenTimestamps 形成外部时间戳 ② 我到手价仍是平台估算，待 New API 真钱校准后替换真值 ③ 官方价需管理员录入(刻意手动=成本真值隔离)。

### 阶段 2 · 变现闭环 + 最锋利竞争力
13. **术值换 credit 兑换池**：✅ **引擎已做**(2026-07-02，金融级对抗审查通过)｜✅ **充值码入口+消费出口已补**(2026-07-03；充值码仅存 HMAC+前后缀，不落明文，创建时必须是 unused 干净码，金额最多 2 位小数且兑换不再 floor 少发，后台不可删除/改金额/回滚已使用记录，仅充值端服务上下文可标记 used)｜✅ **账本并发锁+金额守卫+后台只读**：credit/术值同一用户写入用 `pg_advisory_xact_lock` 串行，credit 流水最多 2 位小数，且 `consume` 必须为负、`recharge/exchange/refund` 必须为正，`credit-logs/contribution-logs/audit-logs` 与用户余额快照字段后台禁止直接增删改，防余额/术值 read-modify-write 丢更新、消费误记入账、余额快照/流水求和漂移与人工改账抹痕｜✅ **兑换开关机器强制**：管理员手填/旧月份毛利不会让 `exchangeEnabled` 生效，必须由本月 `worker:reconcile-newapi` 写入 `marginSource=newapi/local` + `marginReconciledAt`；后台管理员也不能伪造 `marginSource/marginReconciledAt`，手填毛利会自动降级为 `manual` 且清空对账时间；首次初始化只放过安全默认值；未来对账时间不会生效，避免旧毛利被一次误填长期续命。⚠️ **但真钱闭环仍未验收**：New API 真实 quota/毛利/日志对账未校准前，不要上线兑换。
    - 三币地基：Users.creditBalance + `credit-logs` 台账(不变量 creditBalance==SUM(logs)) + `src/lib/credit.ts` applyCredit(原子/幂等/防透支)
    - `economy-settings` 全局(enabled/α/当月毛利占位/兑换率/三层上限) + `src/lib/economy.ts`(池核算)
    - GET/POST `/v1/economy/exchange`(状态+兑换) + 控制台兑换 UI
    - 保命红线落地：兑换池=floor(α×当月已实现毛利分)，credit与分1:1，先赚到才有得兑
    - **对抗审查 P0 修复(真机验证)**：pg_advisory_xact_lock 全局串行化兑换关键区→事务内复核池/术值余额，杜绝并发池超支(亏margin)与术值透支(凭空造credit)；awardContribution 加负余额保护；强制 idempotencyKey 且按 `exchange:<user>:<key>` 作用域隔离，避免跨用户碰撞/误判；clamp α∈[0,1]
    - **新增进展(2026-07-03)**：`POST /v1/economy/recharge` 一次性充值码（HMAC 存储 + 用户级严格限流）+ `worker:reconcile-newapi` 月度用量/毛利回填骨架；`NEWAPI_USAGE_SOURCE=local` 可在无 `/api/log` 权限时用本平台 consume 流水做保守估算；`NEWAPI_USAGE_SOURCE`、`NEWAPI_MARGIN_RATE`、`NEWAPI_MODEL_MARGIN_RATES` 与 `NEWAPI_RECONCILE_TOLERANCE_CENTS` 非法值会 fail-closed，禁止静默回退默认口径；`NEWAPI_USAGE_SOURCE=newapi` 写回已改为 `/api/log` token 用量 × 日志实际倍率，缺失时回退 `/api/pricing`/`/api/status` 价格快照，手填模型毛利率只作 dry-run 对照；`--apply` 会校验 token×price 与 New API quota 实扣、本地 credit consume 的漂移；若走 local 估算，则必须同时满足 `NEWAPI_USAGE_SOURCE=local` + `ALLOW_LOCAL_MARGIN_EXCHANGE=1`，避免 stale local 确认在切回 newapi 后继续生效；`worker:plan-newapi-drift` 可读取逐用户漂移 JSONL 生成只读人工补扣/退款清单，不自动改账。**残余**：① 用当前 New API 用户态 `/api/log/self` 跑通真实小额闭环，并校准 `NEWAPI_CREDIT_TO_QUOTA`、pricing/status 快照与 `NEWAPI_RECONCILE_TOLERANCE_CENTS`；若只能 local 估算，则需书面确认 local 口径与 `ALLOW_LOCAL_MARGIN_EXCHANGE` ② 术值/credit/充值码账本并发集成测试 ③ `monthlyRealizedMarginCents` 单位必须为分，勿填成元(放大100倍) ④ 术值行锁靠全局咨询锁串行(兑换低频可接受)
14. **术值明码标价商城**：⏸ **推迟**（复盘定论：北极星①无基线时曝光是零价值商品；且 verified 插队与护城河第4层信任标记冲突——只卖曝光类不卖信任类）。
15. **省钱路由落地**（护城河第1层⭐）：✅ **前半(数据回写)已做**(2026-07-02) + ✅ **后半-省钱回执已做**(2026-07-02，随#16)：SkillRun.savedAmount(相比默认premium模型省的估算元) + 控制台"本月省钱"统计。✅ **2026-07-03 续补**：per-user 个人化路由已接入，`runSkill` 按 user×skill 历史 `SkillRuns` 聚合出个人够用且更省的模型，`selectModel` 优先个人历史→全站 dataDriven→作者手填。
16. **私人运行台账**（护城河第2层⭐，切换成本主锚）：✅ **已做**(2026-07-02)：控制台运行记录升级为私人台账(details 展开输入/输出/模型/路由/成本 + 省钱标记 + 本月累计省钱头部) + **换模型一键重跑**(`POST /v1/runs/[id]/rerun`，同版本换模型公平对比，复用 runSkill 全套护栏+归属校验)。✅ 2026-07-03 续补：Favorites/Installs 已升级为订阅告警，已发布 Skill 切换当前版本时通知收藏/安装用户，去重并排除作者本人。
17. **通知系统 + 用户设置(自助改模型Key) + 举报处置端**（并入 #16 迭代）：✅ **全部已做**(2026-07-02)：通知系统 + 用户设置(自助设/清 BYOK Key) + **Reports 一键处置端**(`POST /v1/reports/[id]/handle` staff-only 四动作驳回/解决/封禁责任人/隐藏内容 + `/console/moderation` 页)——审核治理闭环成。
18. **返利宝箱**：⏸ **移出主线**——充值码入口已跑通，但返利/随机券仍必须先做法务 review。

### 阶段 3 · 成品化（UX + 质量 + 上线）
19. **前端基建**：✅ 移动导航(汉堡菜单 `MobileNav`) · ✅ error/loading/not-found 边界(全站前台) · ✅ SEO(sitemap/robots + 详情页 generateMetadata，2026-07-02；✅ 2026-07-03 服务端 sitemap/robots/manifest/device auth 改读运行时 `SERVER_URL`，避免换域名必须重 build) · ✅ **列表分页**(2026-07-02，对抗审查通过：共享纯服务端 `Pagination` 组件[首尾±1 窗口/自定义 pageKey 支持一页多榜/越界 page 内部 clamp 防坏链接]；覆盖 Skill市场·悬赏·排行双榜·控制台6tab·我的作品；筛选/排序/搜索切换重置页码) · ✅ **一键复制**(2026-07-02，`CopyButton` 内联"✓已复制"反馈+非HTTPS execCommand回退；接入邀请码分享/校验和核验两处) · ✅ 移除登录页种子凭据(admin12345/WELCOME1，2026-07-02) · ✅ seed 脚本不再生产创建 admin12345/WELCOME1：生产首管必须 `SEED_ADMIN_PASSWORD` 强密码，固定测试邀请码默认跳过(2026-07-03)。**残余[P2]**：详情页评论(limit20)/版本(limit10)未分页，热门 Skill 看不全历史——非命脉，后续做"加载更多"；全局 Toast Provider 未做（当前内联反馈已够，按需再上）。
20. **测试 + CI**：✅**基线已做**(2026-07-02)——Vitest 地基(vitest.config/@别名/node环境)，现 44 文件 293 例覆盖对抗审查修过的安全/正确性纯函数与关键业务不变量；✅ GitHub Actions CI 已跑 node22·npm ci·lint·typecheck·test；✅ 2026-07-03 续补 production build job(Postgres service + `DATABASE_URL`/`PAYLOAD_SECRET`/`NEXT_PUBLIC_SERVER_URL`)；✅ 2026-07-03 再补 `npm run lint` 静态门禁：禁提交模型 sk Key/通用高熵令牌、锁死中立榜收益隔离、兑换幂等/本地毛利确认、生产 mock fail-closed、网关错误脱敏、CSRF 同源边界、封禁用户后台入口 fail-closed；✅ 2026-07-03 再补 credit/术值账本原子写入、幂等跳过、透支回滚、用户锁、流水符号/精度、台账只读追加、余额快照不可手改与有流水用户不可删单测；✅ 2026-07-03 再补价格透明比较单测、分数外锚哈希链/manifest 单测、经济毛利来源防伪造/初始化/local stale/未来时间单测、生产 URL 同源门禁单测、校准临时 quota 清零/新增消费记录/消费不越额/credit 金额边界静态门禁、quota 刻度显式配置/运行时 fail-closed 单测、子令牌无限配额关闭单测、平台调用前 quota 对齐单测、子令牌模型白名单/空白名单/缺分组/非法 TTL/同名重复/深分页截断/缺 quota/type/token_name/退款异常流式/时间窗失效/未来时间日志 fail-closed、New API preflight/CLI token/time/结算样本探测、对账配置/逐用户漂移 JSONL 处理清单 fail-closed 与网络失败隔离/分组漂移/滚动 TTL 修复单测；✅ 2026-07-03 再补逐用户漂移报告 `.jsonl` 路径限制、拒绝软链接、原子替换与 `0600` 权限单测/门禁；✅ 2026-07-03 再补漂移 JSONL 解析一致性/重复行防双补与只读人工动作清单、New API 日志模型/token 字段提取、token×pricing 成本复算、pricing/status 在线预检、手填模型毛利降为 dry-run 的单测与门禁。**残余**：① 真 Payload/Postgres 集成测试(需测试库/Payload Local API，未做) ② 分页纯逻辑已抽到 `src/lib/pagination.ts`(SRP)。
21. **部署上线**：schema 迁移体系 · 密钥强随机 · HTTPS/域名 · cookie Secure · 首管强口令 · 备份 · 健康检查（详见 §6）。
22. ✅ **README 重写**(2026-07-02：更正 v0.1→当前能力/18集合/首用户引导/指向总纲) + ⏳ CHANGELOG/版本纪律。

### 阶段 4 · 抗趋同 + 打磨
23. 任务可靠性层 `aggregateByProfile` + 代际回归告警（护城河第3层）。
24. 可验签中立基准证书 + 黄金样例逐条打分（护城河第4层）。
25. 白标/i18n/无障碍/评论管理/分类页 等增强。

---

## 6. 部署上线要点（平台相关，阶段3 内落地）
仍必须：**生产关 push、启动前 preflight+migrate**（迁移基线已生成；preflight 已做强配置门禁）· **密钥强随机**（你自己配一次）· **HTTPS+域名**（服务端 `SERVER_URL` + 客户端 `NEXT_PUBLIC_SERVER_URL`） · **cookie `Secure`**（`Users.auth.cookies`）· **首管强口令**（走 Payload 首用户引导；seed 生产需 `SEED_ADMIN_PASSWORD` 强密码）· **备份**（pgdata+media 一致性，按 `docs/安全运营SOP.md` 做离机加密与恢复演练；preflight 已阻断未确认项）· **健康检查**（`/health`+compose）· **限流/CSRF**（公网多用户升为必做）。
> 定位翻转后已**移除**：LICENSE 开源协议、密钥首启自举、多架构镜像、换域名不重建、面向陌生部署者文档。
> ✅ 已修：Media 本地卷 · DB idle 超时 · `/health`+compose 健康检查 · cookie Secure · `/v1` 同源 CSRF proxy · Redis 运行分布式限流。

---

## 7. ⚠️ 待作者拍板（否则无法动阶段2 的"钱"）

**A. 真实数值（决定全盘，上线前用你实例小额真兑校准）**：New API `NEWAPI_CREDIT_TO_QUOTA`、日志 `other` 实际倍率、`/api/pricing` + `/api/status` 的模型/分组倍率、`quota_per_unit`、汇率与 quota 实扣偏差；α(0.30)/β(0.15)/免费额度 F(30-50credit)/术值兑换率。
**B. New API 能力（贴一次真实 curl）**：是否 QuantumNous/new-api；建 token 能否设 group/remain_quota/expired_time(TTL)/日调用帽/model_limits；`/api/log/self` 能否按 `token_name=hs_<userId>` 读单子令牌消费明细、模型字段、输入/输出/cache token；`/api/pricing` 与 `/api/status` 能否返回目标分组价格快照。
**C. 合规法务（3 件，必做）**：满额返利+虚拟奖是否规避有奖销售/赌博红线；credit/术值不可提现倒卖的协议条款；额度充值是否算单用途预付卡需备案。
**D. 中立性取舍（书面确认）**：接受榜单如实标"我更贵、BYOK更省"？加速/曝光绝不进随机池仅明码标价？是否愿花钱代跑竞品模型 benchmark？
**E. 反女巫**：verified Runner 开通方式（人工/保证金/实名）？是否追加更强设备指纹/手机号等二阶风控（当前 IP+localStorage deviceHash 只能压低、不能根除）？

---

## 8. 北极星指标 + 残余风险
**北极星**（这两个不起来一切空转）：① 每周有产出的作者数 ② 单位 Skill 累计真实样本数 × 当前窗口新鲜度。（非 Skill 总数/PV 虚荣指标）

**残余风险**（外生、产品无法对冲）：分发即正义（网关顺带看板截流）· solo 在真实用户量的资本消耗战处下风 · 隐私承诺(只存 sizeBucket)封顶护城河深度且与私人台账黏性冲突 · verified 手动置位是规模瓶颈 · 合规最终定性需属地律师 · 冷启动烧固定预算(P_boot+Free_boot≈¥500/月)。

---

## 9. 关键代码锚点（build 时直接定位）
| 事项 | 文件 |
|---|---|
| 聚合层重写(护城河第0层) | `src/lib/compat.ts`（aggregateByModel/recomputeLocalScore） |
| 水龙头/线上评测回流 | **`src/lib/skillRunner.ts`（6b 块，source:'online' 回流+抗刷）**；`compare/route.ts` 仅调用方 |
| register 术值漏洞/反女巫 | `src/app/v1/auth/register/route.ts` + `src/lib/clientMeta.ts` + `src/lib/contribution.ts` + `src/collections/ContributionRules.ts` |
| 省钱路由 | `src/lib/route.ts`（selectModel）+ `routePolicy` + `MODEL_PRICES` |
| 私人台账 | `src/collections/SkillRuns.ts`（inputJson/outputText/model/estimatedCost） |
| New API 对接 | `src/lib/newapiAdmin.ts`（管理 API 抽象，stub/real 双模式✅）· `src/lib/newapi.ts`（chatCompletion） |
| 中立评测榜(新建) | `src/lib/modelrank.ts` + `ModelPriceSnapshot`(新集合) |
| 签名/验签 | `src/lib/signing.ts` · `canonical.ts` · `runner/hengshu.mjs` |
| 术值/悬赏事务范式 | `src/lib/contribution.ts` · `src/collections/Bounties.ts`(frozenPoints/throwOnError/idempotencyKey) |
| 创作者发布 ✅已建成 | `src/components/SkillForm.tsx` · `src/app/(frontend)/console/skills/new/` · `POST src/app/v1/skills/route.ts` |
| credit 台账/兑换 ✅ | `src/lib/credit.ts` · `src/lib/economy.ts` · `src/app/v1/economy/exchange/route.ts` · `src/collections/CreditLogs.ts` |
| 台账对账 worker ✅ | `src/worker/reconcile-ledger.ts`（dry-run/--apply） |

---

## 10. 一句话收尾
**别人复制得了我的静态结论，复制不了我"持续为具体用户的具体 prompt 生成、越用越贵的活体运行资产 + 中立可验签基准 + 自有中转的省钱路由变现闭环"。眼下最优先的两件事：① 用真实 New API 小额闭环校准 `/api/log/self` token 字段、日志实际倍率、`/api/pricing`/`/api/status` 与 quota 实扣偏差（兑换未验收前不开放）；② 在生产跑 20 个官方种子 Skill + benchmark，让真实样本把供给飞轮推起来。**
