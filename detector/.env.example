# Copy to .env and fill in your values
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-opus-4-7

# Public brand/runtime configuration
GEWU_SITE_URL=http://localhost:8765
# Set this to the public repository before deployment (required by AGPL).
GEWU_SOURCE_URL=
# Optional. Leave empty to disable analytics.
GEWU_ANALYTICS_ID=
# Hosted deployments reject localhost/private targets by default. Enable only
# for a trusted self-hosted instance that intentionally tests LAN relays.
GEWU_ALLOW_PRIVATE_TARGETS=0
# Local development only. Production must keep this disabled; HTTPS requests
# work without it after the reverse proxy sets X-Forwarded-Proto correctly.
GEWU_ALLOW_INSECURE_API=1
GEWU_JOBS_DIR=./web_data/jobs
GEWU_WISHLIST_PATH=./web_data/wishlist.txt
